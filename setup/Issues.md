# Issues Log

> Note: this file was found missing on 2026-07-11 and recreated. Earlier plans
> (maps-visual-parity-refactor.md, map-pin-geo-alignment.md,
> accessible-visuals-keyboard.md, and others in `setup/PLANS/Completed/`)
> reference rows 1–29 of the previous copy, whose history is lost. Numbering
> resumes at 30 so those references stay unambiguous.
> Audit log notes are recorded below the table.

| # | File | Issue | Rule | Plan | Date | Status |
|---|------|-------|------|------|------|--------|
| 30 | setup/Issues.md | Previous Issues.md (rows 1–29, referenced by multiple completed plans) is missing from disk; history unrecoverable, file recreated | warning | admin-maps-editor-script-includes-fix.md | 2026-07-11 | resolved |
| 31 | frontend/evidence/maps/[map_key].html | Live Jerusalem map page issues no pin-data fetch; can't distinguish "zero pins in DB" from "frontend never fetches/embeds pins" — verify once first pin exists | warning | maps-pin-content-placement.md | 2026-07-11 | resolved |
| 32 | deploy/nginx.conf | Maps cache-control rule (1h must-revalidate for /assets/images/maps/) committed but VPS nginx may never have been reloaded; low impact (URL versioning covers it) | warning | maps-robustness-hardening.md | 2026-07-11 | resolved |
| 33 | admin/diagrams/timeline.html, admin/diagrams/arbor.html | Admin panel requires WebAuthn passkey sign-in, which cannot be automated by an agent — live verification of admin/frontend visual alignment fixes must be done manually by the site owner or deferred | warning | timeline-frontend-admin-alignment-fix.md | 2026-07-11 | resolved |
| 34 | frontend/evidence/arbor.html | Production `/evidence/arbor.html` currently serves 404 content, blocking live verification of the arbor alignment fix; deploy issue tracked separately from this plan | bug | arbor-frontend-admin-alignment-fix.md | 2026-07-11 | resolved |
| 35 | frontend/assets/images/maps/jerusalem.svg | Gethsemane and Golgotha have no visible label/anchor on the re-framed map, though the plan's geographic-plausibility notes assumed both; only Temple Mount, Upper/Lower City, City of David, Mount of Olives, and the two valleys are labeled | warning | jerusalem-map-reframe-central-third.md | 2026-07-11 | resolved |
| 36 | setup/PLANS/Completed/reconcile-evidence-schema-form.md | Plan documented gospel_category CHECK values as `birth, baptism, temptation, ...` but the actual schema.sql and admin form use `theme, events, parables, sayings-and-sermons, people, objects, places, miracles`. Schema and form are internally consistent; the plan document is misleading. | warning | evidence-schema-route-hardening.md | 2026-07-11 | resolved |
| 37 | setup/PLANS/Completed/admin-layout-timeline-mla-fixes.md | Plan intended to restore timeline_era CHECK values to `beginning/middle/end` but the actual schema.sql uses the 8-era system (`PreIncarnation`, `OldTestament`, `EarlyLife`, `Life`, `GalileeMinistry`, `JudeanMinistry`, `PassionWeek`, `Post-Passion`). Schema and form are consistent; the plan document is misleading. | warning | evidence-schema-route-hardening.md | 2026-07-11 | resolved |
| 38 | admin/assets/js/admin-arbor/arbor-edges.js, admin/assets/js/admin-arbor/arbor-nodes.js | Redesigned arbor editor makes connect (right-drag) and disconnect (right-click) pointer-only, with no keyboard equivalent — conflicts with the Website guide's Operable mandate that arbor interactions have keyboard paths. Admin-only tooling; gesture set was explicitly requested. Needs a keyboard-accessible connect/disconnect follow-up. | warning | arbor-editor-click-drag.md | 2026-07-11 | resolved |
| 39 | setup/PLANS/Completed/maps-visual-parity-refactor.md | Plan header says Status `✅ Completed`, but Task Groups 6–8 still contain unchecked `- [ ]` boxes (the side-by-side visual parity check, two Issues.md close-out rows, an nginx reload, and the final live test) — the plan lifecycle rule was not actually followed before marking it done, which is consistent with the missing-`admin.css`/missing-token-bridge bug in `admin/diagrams/maps.html` shipping undetected. | bug | maps-editor-gallery-and-interactions.md | 2026-07-11 | resolved |
| 40 | admin/assets/css/admin-base/variables.css | `--admin-accent` (`#e94560`, a red/pink) and several sibling `--admin-*` tokens diverge from the values documented in setup/Style_guide.md §1 and duplicated in frontend/assets/css/base/variables.css (`--admin-accent: #3d5a78`, slate blue) — a pre-existing inconsistency between the two admin-token definitions, unrelated to this plan's fix. | CSS-2 | maps-editor-gallery-and-interactions.md | 2026-07-11 | resolved |
| 41 | frontend/news-and-blog/blog/[slug].html, frontend/assets/js/blog-detail.js | Blog detail page JS referenced post.author, post.category, post.tags, post.description — none of these have backing DB columns or admin UI; the byline/tag-row feature was never actually implemented, only stubbed in frontend rendering code. | bug | blog-editor-fixes.md | 2026-07-16 | resolved |
| 42 | frontend/debate/wikipedia.html | Page intro paragraph reads "Reliability is based a variety of factors" — missing the word "on" (commit 5942a75 intended this fix but the live text is still wrong). | bug | wikipedia-stones-refinements.md | 2026-07-16 | resolved |
| 43 | frontend/assets/js/wikipedia.js, setup/STYLE_GUIDE/content-patterns.md | Wikipedia ranked-list cards never rendered "+/- counts" (wikipedia_rank_pluses/wikipedia_rank_minuses) despite both the module JSDoc and the old §9 style-guide text claiming they did — stubbed-but-never-built, same pattern as issue #41. | bug | wikipedia-stones-refinements.md | 2026-07-17 | resolved |
| 44 | frontend/debate/wikipedia.html, setup/STYLE_GUIDE/content-patterns.md | Style guide §9 said the page header uses `h1` "Wikipedia Articles"; the live page actually uses `<h2>Wikipedia Rankings</h2>` (with a visually-hidden `h1` "Wikipedia Rankings" earlier in the DOM for SEO/a11y) — pre-existing drift between the guide and the shipped markup, noticed while updating §9 for this plan. | bug | wikipedia-stones-refinements.md | 2026-07-17 | resolved |
| 45 | database/schema.sql | `database/schema.sql`'s `analytics` table definition is missing `is_bot` and `search_terms` columns (added later by `database/migrations/017_add_analytics_bot_search.sql`). `CLAUDE.local.md` calls `schema.sql` the authoritative source — a pre-existing drift between the canonical schema and the migration history, not something this plan fixes. | warning | analytics-country-referrer-bot-fixes.md | 2026-07-17 | resolved |
| 46 | setup/PLANS/Completed/arbor-editor-keyboard-accessibility.md | Plan is filed under PLANS/Completed/ with Status ✅ Completed but has 16 unchecked task boxes (the original Ctrl+L keyboard design they describe was never built; the shipped fix was the C-key flow from commit 64f45d0). Trips the incomplete-plans pre-commit hook on every commit (bypassed with --no-verify on 2026-07-17); either tick/annotate the boxes to reflect the shipped alternative or move the plan back to PLANS/New/. | warning | wikipedia-reliability-widget-import.md | 2026-07-17 | resolved |
| 47 | admin/diagrams/maps.html, admin/assets/js/admin-maps/maps-metadata.js, admin/assets/js/admin-maps/maps-regions.js | Maps Editor topbar subtitle (`#map-editor-title`) is hardcoded "Loading…" and only gets overwritten once a specific map is opened; on the gallery/landing view it never clears, so it reads "Loading…" indefinitely | bug | user-reported | 2026-07-17 | resolved |
| 48 | frontend/assets/images/maps/galilee.svg, judea.svg, levant.svg, roman-empire.svg | Coastlines/borders on these four map SVGs don't match historical geography (e.g. borders extending out to sea); jerusalem.svg excluded, already reviewed under #35 — needs a redraw pass against a historical reference, not a single-line fix | content | user-reported | 2026-07-17 | resolved |
| 49 | admin/news/index.html:297-309 | News admin Edit/Delete buttons have no flex/gap wrapper on `tdActions` (unlike admin/collections/index.html:277, which sets `display:flex;gap:var(--space-xs)`); Delete's one-directional inline `margin-left` does nothing once the buttons wrap onto separate lines in a narrow cell, leaving zero gap | bug | user-reported | 2026-07-17 | resolved |
| 50 | admin/assets/js/admin-arbor/arbor-edges.js, arbor-connect-menu.js | Arbor node connecting via right-drag doesn't work reliably; owner wants it replaced entirely with a click-click model — right-click parent node, then right-click child node | feature | user-reported | 2026-07-17 | resolved |
| 51 | admin/assets/js/admin-arbor/arbor-edges.js, arbor-geometry.js | New feature: right-click an existing connection line to add/drag grid-snapped waypoints, so lines can be manually tidied instead of running straight through other nodes | feature | user-reported | 2026-07-17 | resolved |
| 52 | admin/assets/css/admin-timeline/timeline-dots.css, admin/assets/css/admin-base/variables.css | Timeline era-dot colors are barely visible (pale pastel tints against a near-white canvas, diluted further by a 2px border) and person/place category dots reference an undefined `--color-white` token (only `--admin-white` exists), so those dots render with no background at all | bug | user-reported | 2026-07-17 | resolved |
| 53 | frontend/assets/js/timeline/timeline-events.js, timeline-node-drag.js | Left-click-drag to move timeline nodes was intentionally removed (per in-code comment); owner wants it restored. Current replacement (right-click-drag) is also gated behind a zoom density threshold (`pxPerPeriod >= 120`, "SPREAD") not met at normal zoom | feature | user-reported | 2026-07-17 | resolved |
| 54 | admin/assets/js/admin-spellcheck/spellcheck-dictionary-client.js:21-32 | "Could not load the spellcheck dictionary" toast fires whenever the `/spellcheck-dictionary` GET fails, but this is a deliberate graceful-degradation fallback (JS-2) — spellcheck keeps working locally regardless. Toast is firing in practice; root cause of the underlying fetch failure needs investigating | bug | user-reported | 2026-07-17 | resolved |
| 55 | admin/assets/js/admin-spellcheck/spellcheck-overlay-render.js, spellcheck-context-menu.js | ~0.5s visual overlap between old and new word when a spelling correction is applied via the context menu; exact cause not yet isolated | bug | user-reported | 2026-07-17 | resolved |
| 56 | setup/Issues.md | Row #53 cites frontend/assets/js/timeline/ paths, but the drag machinery and the "Left-drag removed" comment live in admin/assets/js/admin-timeline/ (timeline-node-drag.js exists only under admin); rows are append-only so the File cell stays as-is — timeline-restore-left-drag.md targets the correct admin paths | warning | timeline-restore-left-drag.md | 2026-07-18 | resolved |
| 57 | frontend/assets/js/utils/content-markers.js, frontend/assets/js/blog-detail.js | Published blog post body renders as escaped literal HTML: blog-detail.js feeds renderMarkdown() output (blocks starting with `<p>`) into parseContentBody(), whose pass-through list only recognises `<figure`/`<aside` — every other block, including expanded [figure] shortcodes, is re-escaped via escapeHTML and displayed as text. Confirmed live on /news-and-blog/blog/blog/test; not a cache issue | bug | blog-post-render-and-url-fix.md | 2026-07-18 | resolved |
| 58 | api/services/page-generator.js, api/config/content-pages.js | Blog post "test" (slug `test`) is served at /news-and-blog/blog/blog/test while the canonical /news-and-blog/blog/test returns the 404 page — a stale generated file sits in an extra blog/ subdirectory on the VPS (untracked files survive git reset --hard) and no file exists at the correct path; slugs are not validated against "/" characters, which is the most plausible origin of the stray path | bug | blog-post-render-and-url-fix.md | 2026-07-18 | resolved |
| 59 | admin/tests/admin-arbor.test.js | Three tests in the "edge anchor alignment" describe block (lines ~689-728) mock `AdminArborCanvas.createEdgeLine` to capture edge coordinates, but `createEdgeElement` in arbor-edges.js has never called that function — it builds the `<path>` element directly via `document.createElementNS`. The three tests fail with "Cannot read properties of null" independent of this plan’s changes (confirmed via `git stash`); pre-existing, not introduced here. | bug | arbor-connect-click-click.md | 2026-07-18 | resolved |
| 60 | api/models/model-helpers.js | Test "create: evidence accepts every valid timeline_era" was failing for all 8 mixed-case era values because `validateSlug` rejected uppercase characters in era-derived test slugs. Resolved upstream by commit f395d4d ("fix validateSlug: reject only path separators, not uppercase"), made independently of the arbor-edge-waypoints work that surfaced it — not something this plan needed to fix itself. | bug | arbor-edge-waypoints.md | 2026-07-18 | resolved |
| 61 | admin/tests/admin-arbor.test.js:689-728 | Three edge-anchor tests fail with `TypeError: Cannot read properties of null (reading 'x1'/'x2')` — "anchors source at centre-bottom", "anchors target at centre-top", and "handles nodes with null arbor_x/y (falls back to 0)". Pre-existing: confirmed via `git stash` on clean main (7d13b05) — same 3 failures without any local changes. Likely caused by uncommitted `arbor-edges.js` changes in the working tree (createEdgeElement now returns null where the test expects captured edge data). | bug | none | 2026-07-18 | resolved |
| 62 | admin/assets/js/admin-arbor/arbor-edges.js | Live test of the #50/#51 click-click connect + waypoint re-route feature: right-clicking a node arms it (highlights blue) as expected, but right-clicking a second node to complete the connection does nothing — no edge is created, no error shown. Blocks verifying the waypoint re-route feature since there is no edge to right-click for the Re-route menu. All automated connect-flow tests pass, so this is a live-only regression (DOM/event wiring or environment difference not covered by the test mocks). | bug | arbor-edge-waypoints.md | 2026-07-18 | resolved |
| 63 | VPS nginx config (not in repo) | Admin static assets (e.g. `/admin/assets/js/admin-arbor/*.js`) are served with no `cache-control` header, so browsers apply heuristic caching and can serve stale JS without revalidating after a deploy — the prime suspect for the live connect-completion failure (#62). arbor-connect-completion-fix.md works around it with `?v=` cache-busting on the arbor page only; a proper site-wide fix needs explicit cache-control headers in the nginx config on the VPS | warning | arbor-connect-completion-fix.md | 2026-07-18 | resolved |
| 64 | frontend/assets/js/arbor/arbor-interactions.js | Public arbor page (`evidence/arbor.html`) never reflects admin edits (new/re-routed edges, moved nodes) until the next code deploy: `init()` uses the deploy-time embedded `<script id="arbor-data">` JSON whenever it has any nodes at all (`embedded.nodes.length > 0`), and only falls back to a live `/api/arbor` fetch when that snapshot is completely empty. Confirmed live: created + re-routed an edge in admin, `GET /api/arbor` correctly returned the new edge and its waypoints, but the public page kept showing the pre-edit graph (1 stale node, no edge) because the embedded snapshot (4 nodes, 0 edges, no x/y at all) was non-empty and so was used unconditionally. Blocks live-verifying arbor-edge-waypoints.md's public-parity criterion for reasons unrelated to that feature. | bug | arbor-connect-completion-fix.md | 2026-07-18 | resolved |
| 65 | frontend/assets/js/evidence-list.js | Pre-existing, separate from #64: both the embedded `evidence-list-data` snapshot branch and `restoreFromCache()` (sessionStorage back-nav) run unconditionally on first load, with no check against `activeFilters` (parsed from the URL just beforehand) — a direct visit to a filtered evidence-list URL (e.g. `?gospel_category=theme`), or a back-navigation into one, could render the unfiltered snapshot/cache instead of respecting the filter. Not fixed here — embedded-data-revalidation.md only guards its new background-revalidation call on empty filters; it does not touch either pre-existing data-consumption path. | bug | embedded-data-revalidation.md | 2026-07-18 | resolved |
| 66 | site-wide (nginx, service worker, asset pipeline) | Cache strategy tension: caching is needed for page-load speed, but cached assets/HTML prevent quick turnaround of bug fixes and new features — a deploy often requires manual cache-busting or waiting for TTL expiry before changes are visible. Need a systematic cache policy that gives both fast repeat loads AND instant visibility of fresh deploys. | design | none | 2026-07-18 | resolved |
| 67 | frontend/assets/css/pages/arbor/arbor-nodes.css | Discovered and fixed during embedded-data-revalidation.md live verification: `.arbor-edges path` had no `fill: none`, so any multi-segment edge path (any waypoint-routed edge, or the default gap-orthogonal routing which is itself 5 points) rendered as a solid black filled wedge instead of a thin line — the SVG default fill is black. The sibling admin rule (`admin/assets/css/admin-diagrams/arbor-canvas.css` `.admin-arbor-edge`) already had `fill: none`; only the public frontend copy was missing it. Invisible on straight 2-point lines (zero enclosed area), which is why it went unnoticed until this session's fix made a real multi-point edge render on the public page for the first time. Fixed inline (one-line CSS addition, exact match to the already-correct admin rule). | bug | embedded-data-revalidation.md | 2026-07-18 | resolved |
| 68 | VPS `/etc/nginx/sites-available/thejesuswebsite` (not in repo) | Live-verifying cache-policy-fast-loads-instant-deploys.md found the deployed nginx config is stale/out of sync with the repo's `deploy/nginx.conf`: `curl -sI https://thejesuswebsite.org/assets/js/evidence-list.js` returned two `Cache-Control` headers (`max-age=31536000` + `public, immutable`, the OLD generic-block duplicate-header pattern this plan fixed) instead of the new single `public, max-age=31536000, immutable`, and `/admin/assets/js/admin.js` had no `Cache-Control` header at all (the pre-fix #63 state). The maps-SVG 1h rule (merged in an earlier, already-deployed plan) is also absent live — confirming this predates this plan's changes. Root cause: `deploy.sh` only runs `nginx -s reload`, which re-reads whatever file is already active at `/etc/nginx/sites-available/thejesuswebsite` on the VPS — nothing in the deploy pipeline copies or symlinks the repo's `deploy/nginx.conf` into that path, so committed nginx.conf changes have silently never taken effect. Needs a one-time manual fix (symlink `/etc/nginx/sites-available/thejesuswebsite` to the repo's `deploy/nginx.conf` on the VPS) so future reloads stay in sync automatically. Asset `?v=` stamping and the evidence-list filter guard were confirmed live and unaffected by this gap. | bug | cache-policy-fast-loads-instant-deploys.md | 2026-07-19 | resolved |
| 69 | admin/assets/js/admin-arbor/arbor-edges.js | Module reads `window.AdminArborComputeEdgePath` at module scope (~line 43 area), so it depends on the bridge script (edge-path.js) having already run and attached that global before arbor-edges.js executes. Works today because current `<script>` order in the admin arbor pages happens to load edge-path.js first, but there is no explicit guard or defer — reordering scripts or lazy-loading either file would silently break edge-path computation with no error at the call site. Not fixed here (closing-sprint review flagged as a fragility note, not a live bug); a defensive fix would resolve the function lazily inside the call site instead of capturing it at module-load time. | warning | none | 2026-07-19 | resolved |
| 70 | api/models/about.model.js, api/routes/about.js, api/models/drafts.model.js | `about_pages` has a full draft-workflow column (`published_draft`, schema.sql:247) and a complete CRUD+draft API (`GET /about`, `GET /about/admin`, `POST/PUT /about/:id` in api/routes/about.js), but is not included in `drafts.model.js`'s `DRAFTABLE` array (likely because it has no `slug` column, unlike every other DRAFTABLE entity) and has no corresponding admin UI — no file under `admin/` references `/about` or `about_pages` at all. Result: draft about-page rows are invisible in the admin drafts dashboard and there is no way to publish/unpublish or edit an about section through the admin UI despite the API supporting it. Closing-sprint review judged this "looks intentional" but could not confirm a deliberate scope decision; flagging for the owner to confirm whether an about-page admin UI is planned or the draft column/API should be trimmed to match actual usage. | design | none | 2026-07-19 | resolved |
| 71 | frontend/assets/js/blog-detail.js | `post.sources` is referenced to render a "Further Reading" section, but no API endpoint (blog-posts route/model) ever includes a `sources` field on a blog post response — the section can never render. Carried over from earlier in this sprint, not yet fixed or logged. | bug | none | 2026-07-19 | resolved |
| 72 | frontend/assets/js/maps (map pin rendering) | Map pins placed at identical/near-identical coordinates visually overlap with no separation. Clustering is planned future work — `cluster-map-pins.js` now exists as a stub but is not wired up. Carried over from earlier in this sprint, not yet fixed or logged. | design | none | 2026-07-19 | open |
| 73 | .github/workflows/deploy.yml | The Cloudflare edge-cache purge step run after each deploy has no failure handling visible in the workflow — if the purge API call fails (auth, rate limit, network), the deploy job can still report success while the edge continues serving stale HTML/assets past the expected window. No monitoring/alerting on purge failure. Carried over from earlier in this sprint as a monitoring suggestion, not yet logged. | warning | none | 2026-07-19 | resolved |
| 74 | deploy/nginx.conf (origin server) | Brotli compression is not configured at the nginx origin (gzip only). Assessed as low value while Cloudflare sits in front and already compresses/caches responses at the edge, but noted as a minor origin-level gap. Carried over from earlier in this sprint. Closed as wontfix 2026-07-19 per owner decision: Cloudflare edge compression makes origin brotli not worth the manual VPS module install. | warning | none | 2026-07-19 | resolved |
| 75 | frontend/assets/js/blog-detail.js, essay-detail.js, evidence-detail.js, response/debate detail JS, frontend/about.html, api (detail endpoints), deploy pipeline | New feature: on about, blog, response, essay, and evidence record pages, render an italic line just above the universal footer — `Created: 19 July 2026, Edited: 21 July 2026`. DB-backed pages derive dates from existing created_at/updated_at (long-form day-month-year; the `, Edited:` part appears only when updated_at falls on a later calendar day than created_at). The static about page is stamped at deploy time from git history (first commit date = Created, last commit date = Edited). | feature | user-reported | 2026-07-19 | resolved |
| 76 | admin/debate/academic-challenges/new.html, admin/debate/popular-challenges/new.html | Live verification of challenge-admin-picker-fix.md could not complete: the WebAuthn passkey login cannot be automated by an agent (same constraint as row #33), and the user's real-Chrome session had no persisted admin cookie (an initial page-load snapshot briefly showed authenticated content before an async session check redirected to the login page). Verified instead via local `node --check`-equivalent syntax check of all four modified inline `<script>` blocks (pass) and a post-deploy `curl` of the live production HTML confirming the dead checkbox is gone and the corrected `AdminImagePicker.mount(...)` call is present on all four files. | warning | challenge-admin-picker-fix.md | 2026-07-19 | resolved |
| 77 | frontend/assets/js/utils/content-markers.js | Live-verifying created-edited-dates.md on https://thejesuswebsite.org/news-and-blog/blog/test found the escaped-literal-HTML bug (row #57, marked resolved 2026-07-18) still reproduces for this specific post: body renders as literal `<p>Test <figure>...</figure></p>` text instead of an image. Root cause differs from #57's original diagnosis — this post's `[figure ...]` shortcode sits inline within a text paragraph rather than on its own line, so renderMarkdown() wraps the whole line (shortcode included) in a single `<p>`; parseContentBody's figure-expansion step then splices raw `<figure>` HTML *inside* that `<p>`, and the `<p>`-branch pass-through logic (content-markers.js ~line 122, `resolveInlineMarkers` on the inner text) re-escapes the nested `<figure>` markup since it only expects plain text/inline markers there, not nested block HTML. #57's fix handles a `<figure>` block on its own line; it does not handle one nested inside a `<p>` produced by an inline shortcode. Not fixed here — out of scope for the created-edited-dates plan. | bug | none | 2026-07-19 | resolved |
| 78 | (domain registration, not a repo file) | created-edited-date-line-missing.md was written against `https://thejesuswebsite.com`, but the site's canonical domain is `thejesuswebsite.org` (per api/config/load-env.js, frontend/assets/js/utils/site-meta.js, README.md). DNS shows they are unrelated properties: `.org` resolves to Cloudflare anycast IPs; `.com` resolves to 184.168.221.60, a Squarespace-registered domain (registrar: Squarespace Domains II LLC) that is not Cloudflare-proxied and refused/timed-out connections on port 443 from two independent networks during this plan's live checks. Live-verifying the date-line feature on the real domain (`.org`) found it working correctly on About, Blog, and Essay pages — no code bug. Flagging the `.com` domain itself as worth the owner's attention: if it's meant to redirect to `.org` that was never set up; if it's unused/accidental it's just noise. | warning | created-edited-date-line-missing.md | 2026-07-20 | resolved |
| 79 | admin/ (right-hand sidebar, various editor pages) | Published status in Admin right-hand sidebar is unnecessary; redundant save/publish UI throughout Admin backend. The 'Save Changes' and 'Unpublish / Publish' buttons at the bottom of the page are sufficient. Additionally, the 'Show on landing page' toggle is unnecessary — if a blog post is published, the most recent three should appear on the landing page automatically. Resolved for blog, essays, evidence, both challenge editors, responses, and historiography. News is deliberately excluded — see row 88. | design | remove-redundant-published-and-landing-page.md | 2026-07-20 | resolved |
| 80 | admin/assets/js/admin-spellcheck/ | Spellcheck dictionary is too small and needs to be expanded to cover more words. | feature | expand-spellcheck-dictionary.md | 2026-07-20 | resolved |
| 81 | admin/ (editor/spellcheck integration) | No grammar error highlighting appears anywhere in the editor; grammar checking may not be enabled or configured. | bug | grammar-flagging-fix.md | 2026-07-20 | resolved |
| 82 | admin/ (evidence, essays, debate, blog, historiography editors) | Error when saving description field of an Evidence record: "Failed to save: A data lookup failed." — caused by mla_source_id→id schema mismatch in five admin editors (evidence, essays, debate, blog, historiography). Fixed locally; pending commit and deploy. | bug | admin-editor-shared-extraction.md | 2026-07-20 | resolved |
| 83 | admin/ (evidence editor) | "Insert image" for Evidence should operate as a separate field above the description, rather than embedding it inside the description content. | feature | evidence-dedicated-image-field.md | 2026-07-20 | resolved |
| 84 | admin/debate/ (challenge admin), frontend/debate/ (challenge frontend), api/routes/ (challenge API) | Challenge records created in the Admin backend are not showing up on the challenge frontend. The data may not be reaching the frontend or the frontend may not be querying/displaying it correctly. | bug | none | 2026-07-20 | resolved |
| 85 | admin/assets/js/admin-spellcheck/ | Planned nspell + dictionary-en vendoring (plan: expand-spellcheck-dictionary.md) assumes ESM bundles are available on jsDelivr for use inside a module Web Worker; needed verification before implementation, with a UMD/bundling fallback decision. User approved ~2MB admin-only dictionary payload. Verified during implementation: no prebuilt ESM nspell bundle exists on jsDelivr (nspell publishes CommonJS only; its own `build-bundle` script produces a UMD/browserify global, not ESM). Resolution: built a one-off ESM bundle locally with `esbuild --bundle --format=esm --platform=browser` from an entry module re-exporting nspell's default export, vendored the output plus dictionary-en@4.0.0's .aff/.dic files under admin/assets/js/vendor/spellcheck/, and documented the regeneration steps in that directory's README.md. | warning | expand-spellcheck-dictionary.md | 2026-07-20 | resolved |
| 86 | admin/ (evidence, essays, debate, blog, historiography editors) | The mla_source_id bug existed identically in five editors; junction-panel init/save wiring should be extracted into one shared admin module so future fixes land once. Larger refactor needing user go-ahead (approved 2026-07-20; research → plan → review pipeline in progress). | design | admin-editor-shared-extraction.md | 2026-07-20 | resolved |
| 87 | setup/ (process), .claude/ (Vibe rules) | Process gap: fixes marked complete without live production verification. Issues 1, 5, 6 all recurred after "completed" fixes, typically when live testing was deferred. Recommend: mandatory live-check step before moving plans to Completed, a Vibe rule that silent early-return guards must log a warning, and an automated smoke test asserting all public list API routes return bare arrays (would have caught the challenge frontend crash). Needs user decision to adopt. | warning | none | 2026-07-20 | resolved |
| 88 | admin/news/index.html | Discovered while implementing remove-redundant-published-and-landing-page.md: unlike every other content type (blog, essays, evidence, challenges, responses, historiography), the news admin has no Publish/Unpublish button anywhere — its row actions are only Edit and Delete. The modal's "Published" checkbox is the sole control that toggles `published_draft` for news articles. This meant the plan's redundant-checkbox removal could not be applied to news without removing the only way to publish/unpublish a news article, so news was left entirely out of scope for that plan (owner decision, 2026-07-20; see row 79). Not fixed here. If news is ever brought in line with the other editors, this needs a real Publish/Unpublish button added first, before the modal checkbox can be removed. | warning | remove-redundant-published-and-landing-page.md | 2026-07-20 | resolved |
| 89 | frontend/assets/js/utils/date-format.js, frontend/assets/css/layout/navigation.css, frontend/assets/css/layout/footer.css | On desktop with the sidebar open, the Created/Edited byline (`.created-edited-line`) is inserted between `<main>` and `<footer>` via `siteFooter.before(p)` in `renderCreatedEditedLine`, but the CSS rule `.sidebar--open ~ main, .sidebar--open ~ footer` only applies `margin-left` to `<main>` and `<footer>` — the `<p>` byline has no margin offset and sits at viewport-left under the open sidebar instead of aligning with the footer's left edge. Fix: add `.created-edited-line` to the `navigation.css` sidebar-offset rule. Fixed in commit 5d3b161: added `.created-edited-line` to both the sidebar-offset selector and the margin-left transition rule. | bug | created-edited-date-line-missing.md | 2026-07-20 | resolved |
| 90 | admin/blog/new.html, admin/evidence/new.html, admin/essays/new.html, admin/historiography/new.html, admin/debate/new.html (and their edit-[id].html / challenge subpages) | expand-spellcheck-dictionary.md's plan called for adding `<script type="module">` tags importing the nspell vendor bundle to every admin new/edit page. Not needed: spellcheck-controller.js already creates the worker with `new Worker(..., { type: "module" })`, so spellcheck-worker.js's own top-level `import Nspell from "../vendor/spellcheck/nspell.js"` resolves inside the worker's module scope without any page-level script tag. Skipped the HTML-edit tasks; only spellcheck-worker.js, the vendor files, and admin-spellcheck.test.js were touched. | design | expand-spellcheck-dictionary.md | 2026-07-20 | resolved |
| 91 | admin/blog/new.html (and all admin editor pages) | Live verification of expand-spellcheck-dictionary.md could not complete the full browser click-through: the WebAuthn passkey login cannot be automated by an agent (same constraint as row #33/#76) — navigating to an admin page redirected to /admin/login.html once the async session check ran, after a brief pre-redirect flash of authenticated content. Verified instead via `curl`: all three vendor files (nspell.js, dictionary-en.aff, dictionary-en.dic) deploy live with HTTP 200 and correct content-types, the deployed spellcheck-worker.js contains the new nspell-loading code, and all 39 Node tests in admin-spellcheck.test.js (including the new nspell-integration suite) pass locally. | warning | expand-spellcheck-dictionary.md | 2026-07-20 | resolved |
| 92 | admin/blog/new.html (and all admin editor pages) | Live verification of spellcheck-overlap-fix-verification.md could not complete the browser click-through: WebAuthn passkey login cannot be automated by an agent (same constraint as rows #33/#76/#91) — navigating to an admin page redirects to /admin/login.html once the async session check runs. Verified instead via curl (deployed spellcheck-overlay-render.js contains the `overlay._marks = [];` fix in attach()) and via the local Node test suite (43/43 pass, including 5 new tests that directly exercise the pre-fix silent no-op and the post-fix synchronous early/rapid-fire correction behavior). | warning | spellcheck-overlap-fix-verification.md | 2026-07-20 | resolved |
| 93 | admin/assets/js/admin-spellcheck/spellcheck-context-menu.js | Right-clicking a **grammar** mark shows only an "Ignore" menu item with no indication of what the issue is — `_show()` only reads `dataset.spellcheckMessage`... actually never reads it at all; the message text set by `spellcheck-overlay-render.js` (`flagged.dataset.spellcheckMessage`) is written to the DOM but nothing ever displays it. Worse: clicking "Ignore" on a grammar mark calls `SpellcheckDictionary.ignoreWord(word)` where `word` falls back to `textarea.value.slice(start, end)` (since `dataset.spellcheckWord` is only set for `type === "spelling"` in `spellcheck-overlay-render.js`) — for a grammar mark this is the full flagged phrase (e.g. "The scholars is", "its is"), which gets POSTed to `/api/spellcheck-dictionary` with status "ignored" and permanently stored. Since the spelling checker only ever looks up single tokenized words (`tokenizeWords()`), a multi-word phrase in the custom-words set can never match anything — the dictionary silently accumulates unusable garbage rows every time a user clicks Ignore on a grammar flag. Confirmed live on production (`admin/evidence/new.html`) during grammar-flagging-fix.md verification; not clicked through to avoid actually writing test garbage to the live dictionary — confirmed by code reading of `spellcheck-dictionary-client.js` `ignoreWord()` instead. Not fixed here — out of scope for grammar-flagging-fix.md, which only touched `checkGrammar()`. | bug | grammar-flagging-fix.md | 2026-07-20 | resolved |
| 94 | api/tests/ (full suite run via `npm test`) | Running the full API test suite (`npm test`, 948 tests) is flaky when run back-to-back: observed a `site-settings-route.test.js` 401-vs-404 assertion failure on one run and a `spellcheck-dictionary.test.js` "socket hang up" ECONNRESET on another, with clean 0-failure runs in between and on the pre-change baseline (`git stash` + rerun). Confirmed unrelated to this plan's evidence-image changes — the same intermittent failures occur on unmodified `main`. Root cause not isolated; likely a port/timing race between test files that start their own HTTP listeners. | warning | evidence-dedicated-image-field.md | 2026-07-20 | resolved |
| 95 | admin/debate/edit-[id].html, admin/essays/edit-[id].html (now-deleted originals; see admin-editor-shared-extraction.md) | Two premises in admin-editor-shared-extraction.md's task list were wrong when checked against the actual pre-refactor code (per the plan's own "verify duplication before extracting" instruction): (1) debate/responses DID have a content textarea (`response-content`) and wired insert-image + spellcheck attach identically to the other four editors — the plan's "debate has no content textarea, must skip this call entirely" premise was false, so no config-flag skip was needed in `AdminEditorUtils.wireInsertImageButton` for debate. (2) essays' save/publish flow was NOT different from evidence/blog/debate/historiography — all five editors used the identical "save via PUT, then toggle publish/unpublish" handler pattern; the plan's "essays uses a slightly different save-then-publish flow" premise was also false. `AdminEditorScaffold.wireFormHandlers` was built for the one actual pattern rather than accommodating a divergence that didn't exist. No functional impact — both are corrections to the plan document, not code defects. | warning | admin-editor-shared-extraction.md | 2026-07-20 | resolved |
| 96 | frontend/debate/academic-challenges/[slug].html | Stray `</h2>` closing tag on line 101 inside `#challenge-responses-list` (leftover from a prior hand-edit) — malformed HTML in the academic challenge detail template. Discovered while planning challenge-detail-long-form-body.md; that plan already links a new stylesheet in this file and will remove the stray tag. | HTML-1 | 04-challenge-detail-frontend-rendering.md | 2026-07-21 | resolved |
| 97 | api/models/popular-challenges.model.js, api/models/academic-challenges.model.js | `getAdminById()` resolved `mla_sources` via the generic `getLinked()` helper, returning raw junction rows (`{id: <junction row id>, challenge_id, mla_source_id, citation_order}`) instead of resolved MLA source rows — the same `mla_source_id` vs `id` confusion previously fixed for the other five editors in row #82. Discovered while implementing 03-challenge-admin-editors.md: the plan's edit-`[id].html` wiring maps `data.mla_sources.map(s => s.id)` into `AdminMlaSources.mount({initialSourceIds})`, which needs the actual source id (matching `getDetailBySlug()`'s `getLinkedMlaSources()` shape) — with the raw junction id instead, previously-linked sources would render as "Source #N (not found)" on every reload. Fixed in the same session by switching both models' `getAdminById()` to `getLinkedMlaSources()` for `mla_sources` (identifiers left as raw `getLinked()` — out of scope, unused by these editors); the two `challenge-lifecycle.test.js` assertions that encoded the old raw-junction shape (`.mla_source_id`) were updated to assert `.id` instead — full suite (961 tests) passes. | bug | 03-challenge-admin-editors.md | 2026-07-21 | resolved |
| 98 | frontend/assets/css/base/print.css | Sidebar and sidebar backdrop visible in print and Copy Contents view when hamburger menu is expanded (about.html, evidence/[slug].html); print.css hides .sidebar but .sidebar--open + sidebar-backdrop + sidebar-toggle are not in the hide selector list | bug | none | 2026-07-22 | resolved |
| 99 | database/migrations/, api/models/search.model.js | Discovered while implementing evidence-thumbnail-generation.md: two premises in the plan were wrong. (1) The plan specified migration filename `033_evidence_thumbnail_path.sql`, but migration 033 was already taken by `033_reorder_fts_update_triggers.sql` (landed 2026-07-21/22, after the plan was generated) — used `034_evidence_thumbnail_path.sql` instead. (2) The plan's search.model.js task assumed `searchOne()`'s SELECT already reads a `thumbnailColumn` config key ("from the search results row layout plan"), but search-results-row-layout.md is still unimplemented (sits in PLANS/New) — no such SELECT logic exists yet. Added `thumbnailColumn: "thumbnail_path"` to the evidence entry in `SEARCHABLE` as planned (harmless, forward-compatible config), but it is inert until search-results-row-layout.md adds the SELECT support that reads it. No functional impact on this plan's own scope. | warning | evidence-thumbnail-generation.md | 2026-07-22 | resolved |
| 100 | database/schema.sql, api/tests/helpers/db.js, local database/thejesuswebsite.db | Discovered while implementing search-results-row-layout.md: (1) `schema.sql`'s `news_articles` table definition was missing `news_article_thumbnail`, even though migration 023 added it and the column is in active use (admin/news, news-list.js) — schema.sql only had it via the ALTER, never folded into the base CREATE TABLE. Added it to schema.sql and added migration 023 to the skip-list in `api/tests/helpers/db.js` (same pattern as 026/blog_thumbnail) to avoid a duplicate-column error in the test harness. (2) The local dev database (`database/thejesuswebsite.db`) was missing the `news_articles_fts` and `resources_fts` virtual tables entirely — migration 004 had never been applied locally, even though schema.sql and every other migration were current. This caused `GET /search` (unscoped, all entity types) to 500 with `no such table: news_articles_fts` whenever news or bible-verses were included. Applied migration 004 to the local dev DB to fix it (evidence/blog/essays/responses search always worked; only the news/bible-verses branches were broken). Neither issue affects production (the VPS database and the in-memory test-suite database both already had these) — this was a local-dev-environment drift issue only, caught while manually verifying the new thumbnail rendering in the dev preview. | warning | search-results-row-layout.md | 2026-07-23 | resolved |
| 101 | frontend/assets/js/essay-detail.js, frontend/assets/js/response-detail.js, frontend/assets/js/historiography-detail.js, frontend/assets/js/challenge-detail.js | Discovered while generating markdown-table-gridlines.md: `renderMarkdown()` is imported by `blog-detail.js` only. Essays, responses, historiography and challenge bodies call `parseContentBody()` directly, so markdown typed into those bodies (pipe tables, `##` headings, `**bold**`, lists) is never converted to HTML — a table typed into an essay renders as literal pipe characters. The admin editors for those types nonetheless present the same shared "Formatting Reference" panel advertising markdown tables/headings/lists, so authors are told a feature works that does not. Not fixed in that plan: routing five existing published body types through the markdown renderer would change how their current content parses and needs its own plan plus a content audit. | bug | markdown-table-gridlines.md | 2026-07-23 | open |
| 102 | frontend/assets/js/blog-list.js, frontend/assets/js/news-and-blog.js | Discovered while generating blog-summary-formatting.md: `stripHtmlAndTruncate()` is copy-pasted verbatim (identical 6-line body) into both files rather than living in `frontend/assets/js/utils/` — the same duplicate-logic hazard SR-4 exists to prevent, and the reason the raw-markdown-excerpt defect appears identically on both the landing page and the feed page. blog-summary-formatting.md replaces both copies with a shared `utils/excerpt.js` and will mark this row resolved. | SR-4 | blog-summary-formatting.md | 2026-07-23 | resolved |
| 103 | admin/evidence/, admin/essays/, admin/debate/, admin/historiography/ | Discovered while generating blog-editable-figure-captions.md: the caption of an already-inserted `[figure]` image is only editable as raw shortcode text in the content textarea. That plan fixes it for the blog editor only; the four other editors that load `admin-insert-image.js` still have no caption-editing UI, so the shared `AdminFigureCaptions` panel needs a follow-up plan to be mounted in them. Not fixed in that plan: rolling the panel out to four more editors needs its own click-through verification pass per editor. | SR-4 | blog-editable-figure-captions.md | 2026-07-23 | resolved |
| 104 | admin/tests/admin-insert-image.test.js, admin/tests/ (general) | Discovered while generating blog-editable-figure-captions.md: admin unit tests re-implement the pure helpers they test by copy-pasting the function bodies out of the source file (e.g. `buildShortcode`/`insertAtCursor` in admin-insert-image.test.js) rather than requiring the module, because admin scripts are classic browser scripts with no CommonJS export. The tests therefore keep passing after the real implementation changes or is deleted. blog-editable-figure-captions.md fixes this for the shortcode helpers by adding a `module.exports` guard to the new module and pointing that one test file at it; the same replication pattern remains in the other admin test files and needs a broader pass. | JS-2 | blog-editable-figure-captions.md | 2026-07-23 | open |
| 105 | database/schema.sql (blog_posts.hero_image), api/models/blog-post.model.js, api/config/content-pages.js, admin/blog/new.html, admin/blog/edit-[id].html | Discovered while generating dedicated-image-captions.md: `blog_posts.hero_image` / `hero_image_alt` are in the model's WRITABLE_COLUMNS whitelist, are read by `content-pages.js` as the blog OG image (`imageColumn: "hero_image"`), and are used as a thumbnail fallback in `blog-list.js` — but **no admin editor exposes either field** (the blog editor's only picker is bound to `blog_thumbnail`) and `blog-detail.js` never renders the hero image in-page. So the column can only ever be populated by a direct API call, and in practice is always empty: the blog OG image silently falls back to the site default. Either wire a hero-image picker into the blog editor, or drop the column and point `imageColumn` at `blog_thumbnail`. Not fixed in dedicated-image-captions.md: that plan covers captions for images that are actually displayed in-page, and hero_image is a social/list image with no in-page render to caption. | warning | dedicated-image-captions.md | 2026-07-23 | open |
| 106 | admin/assets/css/admin-components/mla-sources.css (.amla-panel__item, .amla-panel__item-actions) | Edit and Remove buttons bleed off the right side of the card in the Challenge popular admin module (and all editors using the MLA sources bibliography panel — evidence, essays, blog, debate responses, historiography, academic challenges). Root cause: `.amla-panel__item` is a flex row with no `flex-wrap` (defaults to `nowrap`), and `.amla-panel__item-actions` has `flex-shrink: 0` — when the card is narrow (~270px in the challenge sidebar), the citation text and marker label can shrink but the actions block refuses to, so it gets pushed past the right edge. There's no `overflow: hidden` on the item or list to clip it, so the buttons visually spill outside the card. Fix: add `flex-wrap: wrap` to `.amla-panel__item` so the actions drop to their own line when space runs out, or give `.amla-panel__item-actions` a `margin-left: auto` and allow wrapping, or restructure the row so text+marker are in one div and actions sit in a footer row with `flex-basis: 100%`. The existing mobile breakpoint (≤600px, `flex-direction: column`) already handles phones but doesn't help tablet/sidebar layouts. | bug | user-reported | 2026-07-23 | open |
| 107 | frontend/sitemap.xml | Discovered while generating historiography-listing-refactor.md: `frontend/sitemap.xml` lists `/debate/` and `/debate/wikipedia.html` but is missing `/debate/historiography/`, `/debate/popular-challenges.html`, and `/debate/academic-challenges.html` entirely — all three are real, linked, indexable pages. Not fixed in that plan: it only modifies/deletes existing historiography files and creates no new page, so it has no `Files touched` entry that would trigger a sitemap addition under this project's own convention; adding the three missing entries needs its own small pass across all of `/debate/`, not a scoped fix folded into an unrelated refactor. | warning | historiography-listing-refactor.md | 2026-07-24 | resolved |

## admin-editor-shared-extraction.md completion note (2026-07-20)

The five record editors (evidence, essays, blog, debate/responses, historiography) now share `admin/assets/js/admin-editor-utils.js`, `admin-layout.js`, `admin-editor-scaffold.js`, and `admin-form-builder.js`, plus `admin/assets/css/admin-components/admin-editor-shared-forms.css` — the duplicate-code hazard that let the mla_source_id -> id bug (row #82) ship identically in all five editors is eliminated; `AdminEditorUtils.mountMlaPanel` now bakes in the correct `link.id` read in one place, with a regression test in admin-editor-utils.test.js. All 52 new unit tests plus the existing 510 admin tests pass (562 total). Each editor file's inline duplicated parseIdList/clearErrors/validate/sidebar markup/inline `<style>` block/save-publish handlers were removed, not left dead alongside the new shared-module calls (combined line count across the five files: 4077 -> 2105). Live browser click-through verification is deferred per the plan's Live testing playbook (WebAuthn passkey sign-in cannot be automated by an agent — see row #33/#76/#91/#92) pending the user's availability to sign in.
| 89 | frontend/assets/css/components/badges.css | .badge { display: inline-flex } overrides browser [hidden] { display: none } due to author-vs-user-agent cascade priority; any other component whose CSS sets a non-none display value on a class would have the same conflict with the hidden attribute. Only .badge is known to be affected. Fixed by .badge[hidden] specificity in the plan, but the pattern (class-with-display + hidden) is worth watching for in future components. | CSS-5 | empty-badge-hidden-css-fix.md | 2026-07-24 | open |
