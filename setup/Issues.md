# Issues

| # | File | Issue | Rule | Plan | Date | Status |
|---|------|-------|------|------|------|--------|
| 1 | `frontend/evidence/index.html` (and other list-type pages) | `<main>` content renders with no max-width container or side padding — list pages stretch edge-to-edge unlike detail/reading-column pages, which each define their own width-constrained wrapper. | warning | search-page-redesign.md | 2026-07-07 | resolved |
| 2 | `frontend/assets/css/components/empty-states.css` | Site has two competing naming conventions for empty/error state text: `.empty-state-message` (kebab, used by maps/timeline pages) vs `.empty-state__message` (BEM, used everywhere else). This plan makes both work but doesn't unify them. | CSS-4 | search-page-redesign.md | 2026-07-07 | resolved |
| 3 | `frontend/debate/wikipedia.html`, `frontend/debate/academic-challenges.html`, `frontend/news-and-blog/blog/index.html`, `frontend/news-and-blog/news/index.html`, `frontend/news-and-blog/index.html`, `frontend/debate/popular-challenges/[slug].html` | Stray/misplaced closing tags still present (e.g. a `</h2>` spliced mid-sentence into a `<p>`, `<!-- T</h2>oggle chips -->` inside a comment, `<section</h2> class="card-grid">`, `<div id</h2>="challenge-responses-list">`) — the same corruption pattern `setup/PLANS/Completed/markup-corruption-cleanup.md` was written to fix. Several of these exact instances are not covered by that plan's file list at all (new finds). | HTML-3 | list-page-container-and-state-naming.md | 2026-07-07 | resolved |
| 4 | `setup/PLANS/Completed/markup-corruption-cleanup.md` | This plan sits in `PLANS/Completed/` but every task checkbox is still unchecked and its Status field still reads "Plan generated — ready for implementation", not "✅ Completed". Spot-checking several of its target files (e.g. `debate/wikipedia.html`, `debate/academic-challenges.html`, `news-and-blog/blog/index.html`) shows the corruption it describes is still present in the body/comments, though the `<title>` tag fixes for those same files do appear to have been applied. The plan appears to have been partially implemented and then moved to `Completed/` without finishing or updating its own tracking fields. | bug | list-page-container-and-state-naming.md | 2026-07-07 | resolved |
| 5 | `sitemap.md` | `setup/PLANS/New/` section is stale: it lists `prelaunch-bug-fixes.md` and `production-deploy-config.md` under `New/`, but both files actually live in `setup/PLANS/Completed/` (and are missing from the sitemap's `Completed/` list). The two entries should be moved from `New/` to `Completed/` in the tree. | warning | search-exact-and-fuzzy-matching.md | 2026-07-07 | resolved |
| 6 | `evidence_pictures`, `essay_pictures`, `response_pictures`, `historiography_pictures`, `blog_pictures` (all in `database/schema.sql`) and the matching `pictures` handling in `api/models/evidence.model.js`, `essay.model.js`, `response.model.js`, `historiography.model.js`, `blog-post.model.js` | These five child tables and their composite-model wiring are fully functional but dead: `frontend/assets/js/evidence-detail.js`, `essay-detail.js`, `response-detail.js`, `historiography-detail.js`, and `blog-detail.js` never query a `pictures` field — each instead parses `[figure src="..." caption="..."]` shortcodes out of its free-text body/description column (`image-upload-pipeline.md` moved Evidence onto this same shortcode mechanism, so it now joins the other four). Needs a decision: delete all five unused tables/model code, or build a renderer that reads them (and decide how that would coexist with the shortcode mechanism already in use for the same content). | warning | image-upload-pipeline.md | 2026-07-08 | resolved |
| 7 | `setup/Style_guide.md` (§13 Auth) and `admin/auth/` | Style guide specifies an `admin/auth/logout.html` confirmation page that has never existed in the codebase; the auth-hardening plan instead surfaces sign-out confirmation via `login.html?signedout=1`. Decide whether to update the guide to match or create the page. | warning | auth-hardening-and-smooth-sessions.md | 2026-07-08 | resolved |
| 8 | `setup/Website_guide.md` | The guide's workflow and definition-of-done repeatedly require reflecting new/changed files in `sitemap.md` (a project-tree document, distinct from `frontend/sitemap.xml`), but no `sitemap.md` file exists anywhere in the repo — the requirement is unfulfillable as written. Either recreate the file or remove the references. | warning | resources-context-card-grids.md | 2026-07-08 | resolved |
| 9 | `api/tests/helpers/db.js` | The shared test-DB helper fails with `SqliteError: duplicate column name: hero_image` before any test body runs, breaking the entire `api/tests/` suite (`cd api && node --test` fails across nearly every file, not just sitemap) — pre-existing, confirmed via `git stash` to reproduce on a clean tree with no changes from this plan. | bug | resources-context-card-grids.md | 2026-07-08 | resolved |
| 10 | `frontend/contextual-essays/index.html`, `frontend/debate/historiography/index.html` (production only) | Both URLs 404 on production (`https://thejesuswebsite.org/contextual-essays/` and `/debate/historiography/`) even when requested directly as `.../index.html` — confirmed via the site's own "Context" nav link and the "Historiography" card on `/debate/`. `/resources/` and `/debate/` load fine, so this is isolated to these two directories, not a global routing failure. Discovered while trying to live-verify the new "Census of Quirinius" essay and the historiography article published via admin for this plan; both saved successfully in the admin (visible/editable at `admin/essays/edit-[id].html?id=1` and `admin/historiography/edit-[id].html?id=1`, publish toggle checked) but their public pages cannot be reached to confirm. Needs VPS access to diagnose (missing directory, nginx config, or deploy gap) — outside this plan's scope. | bug | resources-context-card-grids.md | 2026-07-08 | resolved |
| 11 | `database/migrations/004_news_and_resource_search.sql`, `database/migrations/004_reconcile_evidence_checks.sql` | Two migration files share the `004_` numeric prefix. Harmless today because tracking is by full filename, but it breaks the implied ordering convention and invites a future collision — renumber one (with a matching `schema_migrations` rename on the VPS) or document that prefixes are non-unique. | warning | context-index-and-open-issues-fixes.md | 2026-07-08 | resolved |
| 12 | `frontend/assets/js/essays-list.js`, `frontend/assets/js/essay-detail.js` | Both files reference `item.title`/`item.author`/`essay.body` but the `context_essays` table returns `essay_title`/`essay_author`/`essay_content`. Same field-name mismatch pattern that `historiography-refactor.md` fixes for historiography — contextual essays have the identical bug and likely render "Untitled" / blank cards until fixed. | bug | historiography-refactor.md | 2026-07-08 | resolved |
| 13 | `admin/debate/new.html`, `admin/essays/edit-[id].html`, `admin/historiography/edit-[id].html`, `admin/debate/edit-[id].html`, `admin/blog/edit-[id].html` | The "Linked Evidence IDs" input exists in the form markup on these five pages, but `buildPayload()` never reads it into the save request — `link_evidence_ids` is present in `admin/essays/new.html`, `admin/historiography/new.html`, and `admin/blog/new.html`, but missing from `admin/debate/new.html` (response creation) and from all four `edit-[id].html` pages (essays, historiography, responses, blog posts). Editing an existing record can never change its linked evidence through this field, and creating a new response can't set it either. Discovered while reading each page's `buildPayload()` to wire in `mla_source_ids` for the MLA Bibliography panel — out of scope for that work. | bug | admin-layout-timeline-mla-fixes.md | 2026-07-08 | resolved |
| 14 | `api/routes/essays.js`, `api/models/essay.model.js`, `admin/essays/index.html`; `api/routes/blog-posts.js`, `api/models/blog-post.model.js`, `admin/blog/index.html` | The public `GET /essays` and `GET /blog-posts` routes pass `req.query` into `getAllPublished(req.query)`, but both model functions take no parameters and always hardcode `published_draft = 1` — the query filter is silently ignored. `admin/essays/index.html` relies on `?published_draft=0` to fetch drafts (returns the same published-only set instead); `admin/blog/index.html` doesn't even attempt a drafts fetch. Net effect: neither admin list page can ever show a draft essay/blog post. This plan fixed the identical problem for historiography by adding a dedicated auth-gated `getAllAdmin()`/`GET /historiography/admin` list endpoint instead of trying to filter the public route — the same fix pattern would resolve essays and blog-posts. | bug | historiography-refactor.md | 2026-07-08 | open |
| 15 | `frontend/assets/js/historiography-detail.js`, `frontend/assets/js/essay-detail.js`, `api/models/historiography.model.js` | `renderBibliography()` expects `article.bibliography` to be an array of pre-formatted MLA citation strings (it interpolates each entry directly into an `<li>`), but the only data available is the raw `*_mla_sources` junction rows (`mla_source_id`, `citation_order`, etc.) — no MLA citation formatter exists anywhere in the codebase. Before this plan `bibliography` didn't exist at all on the historiography detail response, so the section silently stayed hidden (safe by accident). This plan's field normalisation now exposes `mla_sources` as `bibliography` on `getDetailBySlug()`, which is more correct field-naming but means the first historiography essay that actually links an MLA source will render literal `[object Object]` list items instead of a citation. Not triggered by the 8 seed essays (none link sources yet). Needs an actual MLA citation formatter before any historiography (or context) essay ships with bibliography entries. | bug | historiography-refactor.md | 2026-07-08 | open |
| 16 | `.claude/launch.json` (`frontend` config), local dev tooling | The `frontend` preview config serves `frontend/` via a plain `python3 -m http.server` with no `/api` proxy, so no public page's data-fetching JS (historiography, essays, evidence, blog, etc.) can be exercised against a live API in local preview — every fetch to `/api/...` 404s and the page falls back to its error state. `admin` has a working proxy (`dev-proxy.js`) for this reason but `frontend` has no equivalent. Confirmed while trying to visually verify the historiography list page's period-grouped rendering locally; had to fall back to direct API calls instead. Public-page UI changes can only be visually verified against the deployed VPS, not localhost, until this is fixed. | warning | historiography-refactor.md | 2026-07-08 | open |
| 17 | `frontend/news-and-blog/blog/index.html`, `frontend/news-and-blog/news/index.html` | Both hand-written index pages were accidentally deleted in commit `3dfe386` ("Switch license to CC BY-NC, add dynamic footer year, add blog hero image upload") — the commit's own description doesn't mention removing them, and the `.gitignore` un-ignore rules for both files were left intact, confirming the deletion was unintentional. Restored from the pre-deletion revision (`3dfe386~1`) as part of this plan, updating the nav (added the missing Resources link) and footer (MIT licence → CC BY-NC, static year → dynamic `#footer-year` span) to match the current site-wide standard before adding this plan's OG/robots tags. | bug | seo-coverage-section-pages.md | 2026-07-08 | resolved |
