# Issues

| # | File | Issue | Rule | Plan | Date | Status |
|---|------|-------|------|------|------|--------|
| 1 | sitemap.md | The api/ tree is stale — it lists only `credential.model.js` and `passkey.js`, but ~16 models and ~21 routes already exist on disk (evidence, responses, essays, etc.). The sitemap understates what is built. | warning | complete-backend-data-layer.md | 2026-06-29 | resolved |
| 2 | database/schema.sql | The journal-article style spec (Style_guide.md §9) references essay/response columns that do not exist in the schema: `two_column` flag, a DOI/citation field, and an author-bio field. Frontend cannot render these until a schema decision is made. | warning | complete-backend-data-layer.md | 2026-06-29 | resolved — migration `003_journal_article_metadata.sql` adds `two_column`/`doi`/`author_bio` to `context_essays`, `responses`, and `historiography`; `WRITABLE_COLUMNS` in all three models include them, the detail JS renders them (DOI line, author-bio, `.two-column`), and the admin edit forms expose them. `schema.sql` is intentionally the initial baseline (byte-identical to `001_initial.sql`); migrations layer on top, so it should NOT be edited to add these columns — doing so would make migration 003's `ALTER TABLE` double-add and break fresh deploys (verified 2026-07-03). |
| 3 | setup/Website_guide.md | Timeline structure conflicts between source docs: Website_guide.md shows a multi-page tree (beginning/middle/ending.html + zoom-* variants), while Style_guide.md §9 describes a single continuous scrollable timeline with era filter chips. The plan implements the single-page Style Guide version; Website_guide.md tree is now stale and should be reconciled. | warning | frontend-timeline.md | 2026-06-29 | resolved — Website_guide.md rewritten as a high-level module guide; the conflicting per-page tree no longer exists (verified 2026-07-02) |
| 4 | setup/Website_guide.md | Maps structure conflicts between source docs: Website_guide.md lists eleven static map pages (roman-empire/levant/galilee/judea/jerusalem.html each with a zoom-*.html variant), while Style_guide.md §9 "Map View" describes a single full-width map with filterable pins. The plan implements one dynamic `[map_key].html` with JS zoom/pan; the Website_guide.md maps tree is now stale and should be reconciled (same pattern as #3). | warning | maps-frontend.md | 2026-06-29 | resolved — same Website_guide.md rewrite removed the conflicting maps tree (verified 2026-07-02) |
| 5 | admin/assets/css/ | The shared admin shell is not yet built on disk — `admin/assets/css/` holds only the auth `login.css`/`register.css`; there is no `admin.css`, admin `variables.css`, or sidebar layout. The maps admin editor (`admin/diagrams/maps.html`) assumes this shell exists and is blocked until it is created. | warning | maps-admin.md | 2026-06-29 | resolved — `admin.css` plus `admin-base/`, `admin-components/`, `admin-diagrams/`, `admin-layout/` now exist on disk (verified 2026-07-02) |
| 6 | mcp-server/package.json | The MCP server depends on `@modelcontextprotocol/sdk`, a non-visual external dependency — a documented exception to SR-2 (which limits dependencies to visual/display libraries). It is a separate backend module that does not affect site load (SR-3), but needs owner sign-off. | SR-2 | mcp-server.md | 2026-06-29 | resolved — owner (luke.isham@gmail.com) approved the SR-2 exception 2026-07-02; standard MCP protocol library, hand-rolling it would violate JS-3 |
| 7 | admin/resources/index.html | Website_guide.md lists 15 separate admin resource pages (one per `list_key`); the plan consolidates them into a single dynamic page with a category selector, matching the frontend `resources/list.html` consolidation. The guide tree should be reconciled (same pattern as #3/#4). | warning | admin-content-management.md | 2026-06-29 | resolved — 15 separate per-category pages were built on disk (matching the original guide, not the consolidation); moot now that the stale guide tree is also gone (verified 2026-07-02) |
| 8 | LICENSE | Website_guide.md specifies an "open-source license" but does not name one; the specific license (e.g. MIT) must be chosen before `LICENSE` is filled. | warning | shared-and-root.md | 2026-06-29 | resolved — `LICENSE` now contains the MIT License (verified 2026-07-02) |
| 10 | admin/resources/sources.html | Website_guide.md lists `sources.html` as a 15th admin resource page, but the sitemap lists only 14 topic pages (no sources). The audit counted 14 missing, not 15. The implementer should check whether `sources.html` is intentionally omitted from the admin resources section (perhaps it's handled under `api/routes/sources.js` separately). | warning | admin-resource-topic-pages.md | 2026-06-30 | resolved — confirmed `sources.html` is intentionally omitted; sources are handled via `api/routes/sources.js` (verified 2026-07-02) |
| 11 | frontend/debate/historiography.html | The style guide §9 describes historiography pages using `journal.css` for detail pages, but the new `historiography.html` is a **listing** page, not a detail page. The plan uses the ranked-list card pattern (same as challenges) for the listing page, which is consistent with how `popular-challenges.html` and `academic-challenges.html` work. However, the style guide doesn't explicitly describe the historiography listing page pattern — it only covers the detail page. | warning | frontend-resource-and-historiography-pages.md | 2026-06-30 | open |
| 12 | frontend/resources/list-1.html, list-2.html, list-3.html | The sitemap lists 3 resource category pages (`list-1`, `list-2`, `list-3`), but the Website_guide.md lists 15 different named resource categories. The mapping between the numeric `list-N` pages and the named categories needs to be defined — the plan relies on `data-category` attributes but doesn't specify which category name maps to which number. The implementer should decide the mapping. | warning | frontend-resource-and-historiography-pages.md | 2026-06-30 | resolved |
| 13 | admin/drafts/index.html | `draftEditUrl()` routes both `responses` and `historiography` draft-list rows to `admin/essays/edit-[id].html`, but that page is hardcoded to `/essays/...` API endpoints with no `type` param — editing a response or historiography item from the drafts list hits the wrong endpoint (or the wrong row, if a same-numbered essay exists) instead of the correct data. Responses already have a correctly-wired editor (`admin/debate/edit-[id].html`) that the drafts list simply doesn't link to. Historiography has no dedicated admin editor at all — no `admin/historiography/` directory exists. | bug | journal-article-metadata-columns.md | 2026-07-02 | open |
| 14 | 40 files across `frontend/` and `admin/` — full enumerated list in `markup-corruption-cleanup.md` | Widespread markup corruption: a closing tag (usually `</title>`, `</h1>`, `</h2>`, `</p>`, `</span>`, or `</script>`) is duplicated or spliced into headings, paragraphs, title/meta tags, `<link>`/`<script>` tags, an inline `<style>` block, and even HTML comments — e.g. `<h1>Context</h1>ual Essays</h1>`, `<p>...for Jesus of</p> Nazareth.</p>`, `<h2>Contextual Essays</h</h2>2>`, `<!-- Off-canvas navigation</h1> sidebar -->`. Breaks `<h1>` uniqueness/placement (HTML-3), malforms `<title>`/`<meta description>` (SEO), and affects both public frontend and admin edit forms. Likely caused by a batch find-replace or bad automated edit at some earlier point. **Corrected count**: the original 22-file estimate came from one detection heuristic (same tag closing twice on one line); a fuller 5-heuristic sweep across all 89 project HTML files found 40 genuinely corrupted files (18 more the original heuristic missed — corruption split across two lines, or two *different* tag names spliced together) and excluded one false positive (`admin/auth/register.html` — its apparent duplicate `</p>` is two legitimate, complete, adjacent paragraphs). | bug | markup-corruption-cleanup.md | 2026-07-02 | resolved — re-swept all 89 HTML files with targeted spliced-tag/split-tagname heuristics (not just generic adjacent-close matching) and found zero corrupted files; spot-checked `frontend/contextual-essays/index.html` (the file with the example in this row) and its `<h1>` is clean (verified 2026-07-03) |
| 15 | `frontend/news-and-blog/blog/[slug].html`, `frontend/news-and-blog/news/[slug].html` | Both pages have two `<h1>` elements: the `.sr-only` header's (per the site-wide Invisible Header pattern) and a second, visible one in the page body (`<h1 class="blog-title" id="blog-title">` / `<h1 class="news-title" id="news-title">`). This may be intentional — Style_guide.md's Blog Posts section explicitly specifies a visible `h1` title for blog posts, unlike essays/responses/historiography, which start visible headings at `h2`. But Style_guide.md doesn't separately describe News Articles' header the same way, and having two `<h1>` elements on one page conflicts with HTML-3 ("Exactly one `<h1>` per page") and the Invisible Header pattern's stated rationale ("Visible headings begin at h2 so heading hierarchy is correct for SEO without visible duplication"). Needs an owner decision: is the second `<h1>` intentional (and if so, should it be downgraded to `h2` to match the documented pattern, or should Style_guide.md be updated to document the exception explicitly for both Blog and News)? | warning | markup-corruption-cleanup.md | 2026-07-02 | open |
| 16 | frontend/assets/js/utils/templates.js | The `html` tagged template escapes every interpolated value including already-safe HTML from nested `html`/`renderBadge` calls, so composed cards/search results/debate lists render as literal escaped text (e.g. `&lt;h3 class=&quot;card-title&quot;&gt;`). Confirmed by executing `renderCard` in Node. Affects all list pages, search, and debate. Fix planned via a `SafeString` marker + `raw()` helper. | bug | frontend-render-bug-fixes.md | 2026-07-03 | resolved — `templates.js` now defines a `SafeString` class with a `raw()` escape-hatch and `safeJoin()`; `html`/`renderCard`/`renderBadge` skip re-escaping `SafeString` instances (verified 2026-07-03) |
| 17 | frontend/assets/js/blog-detail.js | `[figure src="..."]`/`[pullquote]` shortcodes never render: the body is HTML-escaped first (turning `"` into `&quot;`), then the regex looks for literal quotes and can't match. Same defect in essay-detail.js, historiography-detail.js, response-detail.js. | bug | frontend-render-bug-fixes.md | 2026-07-03 | resolved — all 4 files (`blog-detail.js`, `essay-detail.js`, `historiography-detail.js`, `response-detail.js`) now process `[pullquote]`/`[figure]` shortcodes on the raw text before escaping the surrounding prose, with matching "BEFORE escaping" comments (verified 2026-07-03) |
| 18 | frontend/assets/js/blog-detail.js | Page/OG titles are double-encoded: the title is `escapeHTML`'d before `setSEO`, but `document.title` and meta `content` are text sinks, so "Faith & History" becomes "Faith &amp; History". Same pattern in essay/challenge/historiography/evidence/news/response detail JS (7 files). | bug | frontend-render-bug-fixes.md | 2026-07-03 | resolved — all 7 detail-page `applySEO()` functions (blog, essay, challenge, historiography, evidence, news, response) pass the raw title straight into `setSEO`/`jsonLd` with no `escapeHTML` call; `setSEO` assigns to `document.title`/`meta.content` text sinks (verified 2026-07-03) |
| 19 | api/models/search.model.js, api/routes/search.js | `SEARCHABLE[type]` truthy-check treats inherited prototype keys (`?type=constructor`, `toString`) as valid, building SQL against undefined table names → 500. Present in both the model (line 33) and the route (line 17). Fix: `Object.hasOwn`. | bug | frontend-render-bug-fixes.md | 2026-07-03 | resolved — both `search.model.js:45` and `search.js:23` now guard with `Object.hasOwn(SEARCHABLE, ...)` (verified 2026-07-03) |
| 20 | api/routes/analytics.js | `POST /analytics` accepts a non-string `page` (the length cap only runs `if typeof === 'string'`), which then throws at the better-sqlite3 binding → 500. Needs an explicit string check → 400. | bug | frontend-render-bug-fixes.md | 2026-07-03 | resolved — `analytics.js:19` now returns 400 when `req.body.page` is missing or not a string, before the length-cap loop (verified 2026-07-03) |
| 21 | frontend/ (site-wide) | Content pages are empty shells hydrated by `fetch`; OG/JSON-LD are injected client-side, so non-JS agents and social/link-unfurl bots see only skeleton states. Full runtime SSR / bot-prerendering (API serving HTML with per-request DB-injected meta) conflicts with the static-frontend/separate-API decision in Website_guide.md and is deferred pending an owner decision. The agent-friendly plan ships llms.txt + DB-driven sitemap + static default meta/noscript as the architecture-preserving first step. | warning | agent-friendly-frontend.md | 2026-07-03 | open — owner decision needed on full SSR/prerender |
| 22 | frontend/sitemap.xml, frontend/assets/js/search.js | Detail-URL patterns must be reconciled before the sitemap generator ships: search.js links evidence results to `/evidence/single/{slug}` while some code/docs imply `/evidence/{slug}`. The generator and the `[slug].html` routes must agree on exact paths or the sitemap will list dead URLs. | warning | agent-friendly-frontend.md | 2026-07-03 | resolved — `search.js:136` and `generate-sitemap.js`'s `urlPrefix` map both use `/evidence/single/{slug}` (and the matching prefixes for every other content type); confirmed consistent (verified 2026-07-03) |
| 23 | api/server.js | All public GET content endpoints (evidence, essays, search, etc.) are unthrottled — only passkey and analytics-POST have rate limits. An abusive agent/scraper can flood the site and the FTS-backed `/search`. Fix planned: apply the existing in-memory limiter to public read routes + a stricter `/search` budget + nginx-layer limits. | warning | public-api-rate-limiting.md | 2026-07-03 | resolved — `server.js` now applies a shared `publicReadLimit` (300 req/min) to every public content mount, and `routes/search.js` carries its own stricter 60 req/min limit on top (verified 2026-07-03) |
| 24 | api/middleware/rate-limit.js | The in-memory limiter is per-process and resets on deploy. Fine for the current single-instance pm2 setup, but if the app is scaled to multiple pm2 workers/instances each gets its own store and the effective per-IP limit multiplies by the worker count. At that point the nginx limit_req layer (or a shared store) must be the authoritative limit. Owner decision needed only if/when scaling beyond one instance. | warning | public-api-rate-limiting.md | 2026-07-03 | open — scaling caveat |
| 25 | admin/historiography/ | No admin editor exists for historiography items: `admin/historiography/` is absent and `admin/debate/edit-[id].html` is hard-wired to `/responses/...`, so historiography drafts cannot be edited anywhere. The prelaunch-bug-fixes plan only re-routes historiography drafts away from the wrong (essays) editor to a safe listing fallback; building a real historiography editor (mirroring the essays editor against `/historiography/...`) is a separate feature needing an owner decision. Same underlying gap as Issue #13. | warning | prelaunch-bug-fixes.md | 2026-07-03 | open — owner decision needed |
| 26 | deploy/nginx.conf | The new `location /admin/` block serves the admin panel as public static HTML (all data stays behind the authenticated API, so no data is exposed). Owner should decide whether to additionally gate `/admin/` at the edge — e.g. an IP allowlist or HTTP basic-auth in front of the passkey login — as defense-in-depth before launch. | warning | production-deploy-config.md | 2026-07-03 | open — owner decision needed |
| 27 | admin/ (all pages) | The admin sidebar navigation block is statically duplicated verbatim across ~36 admin HTML pages rather than injected from one shared source, so every new nav entry (e.g. the Historiography link this plan adds) must be edited into all 36 files to keep navigation consistent, and any nav change risks drift. Candidate fix: render the sidebar once from a shared admin JS component. Out of scope for the historiography editor plan. | warning | historiography-admin-editor.md | 2026-07-03 | open |
