"""Section classifier for the Wikipedia v2 ranking pipeline.

Labels every body paragraph as data, interpretation, or neither,
computes the separation ratio, and assigns the row-3 tier contribution.

This package is an offline developer-machine tool. The VPS never runs it.
"""

__version__ = "0.1.0"
