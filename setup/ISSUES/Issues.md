# Issues Log

> Tracks bugs, warnings, design gaps, and feature requests discovered during
> development. Each row links to the plan that resolves it (or `none` if
> unfixed). Resolved rows are archived periodically to `setup/ISSUES/`.

| # | File | Issue | Rule | Plan | Date | Status |
|---|------|-------|------|------|------|--------|
| 111 | database/schema.sql (evidence_resource_lists), api/models/resource.model.js | Discovered while generating resources-module-refactor.md: the `evidence_resource_lists` junction table and the five model functions that operate on it — `addToEvidence()`, `removeFromEvidence()`, `getResourcesForEvidence()`, `reorderEvidenceResources()`, `reorderList()` — are referenced by no route, admin page, frontend module or MCP tool anywhere in the repo (grepped across api/, admin/, frontend/, mcp-server/). The table has an index (`idx_evidence_resource_lists`) and a FK to `resources(id)` but is never written to or read from, so evidence records cannot actually be linked to resource lists despite the schema implying they can. Either the feature was planned and never built, or it was removed and the storage left behind. Not fixed in that plan: deciding between wiring it up and dropping it is a product question, and the dead FK is one reason that plan deliberately avoids rebuilding the `resources` table. | warning | resources-module-refactor.md | 2026-07-25 | resolved |
| 112 | database/migrations/032_challenge_body_and_drop_urls.sql, database/schema.sql | Discovered while generating resources-module-refactor.md: migration 032 runs `DROP TRIGGER IF EXISTS resources_fts_au` and `news_articles_fts_au` to unblock a `DROP COLUMN`, on the stated premise that "their backing virtual tables were removed in a prior migration" — but that premise is wrong: migration 004 creates `resources_fts` and `news_articles_fts`, and migration 022 had just recreated both `_au` triggers to fix a different bug. No later migration recreates them (033 recreates only the four triggers that exist in schema.sql). So on any database that has run 032, editing a resource or news article's title/description no longer updates its FTS index — search returns stale titles until the row is deleted and reinserted. Compounding it, `schema.sql` defines only four FTS virtual tables (evidence, responses, context_essays, blog_posts) and never `resources_fts`/`news_articles_fts`, so a database created fresh from schema.sql lacks them entirely and `GET /search` 500s on those entity types — the same drift already recorded as row #100 for the local dev DB, but the root cause is the schema.sql gap, not local drift. Not fixed in that plan: restoring the two triggers needs its own migration plus a decision on folding both FTS tables into schema.sql, and it affects news search as much as resources. | bug | resources-module-refactor.md | 2026-07-25 | resolved |
| 113 | setup/PLANS/New/admin-test-import-real-modules.md, admin/tests/admin-ranking.test.js | Plan-review pass on admin-test-import-real-modules.md found its audit grep too narrow: it searches admin test files only for the header phrases "reproduced inline", "synced copy", or "re-implement", which misses `admin-ranking.test.js` (whose header says "Replicate the pure function from admin-ranking.js") — a broader search (e.g. any of replicate/copy/duplicate/sync, case-insensitive) is needed before the plan's fix task can find every affected file. | JS-2 | admin-test-import-real-modules.md | 2026-07-25 | resolved |
| 114 | setup/PLANS/New/badge-hidden-css-closeout.md | Plan-review pass on badge-hidden-css-closeout.md found the Completion Protocol references a final "push everything to GitHub" step that has no corresponding checkbox task in the Deploy & verify section, and that section's Tier-1/Tier-3 items are listed as a flat numbered list rather than the template's unified task-group structure — cosmetic gaps, not blockers. | design | badge-hidden-css-closeout.md | 2026-07-25 | resolved |
| 115 | setup/PLANS/New/blog-hero-image-to-thumbnail.md, api/tests/journal-content.test.js | Plan-review pass on blog-hero-image-to-thumbnail.md found it removes `hero_image` from WRITABLE_COLUMNS but has no task to update or remove the three tests in `journal-content.test.js` that assert hero_image round-trips through create/update/getAdminById — those tests will start failing once hero_image is stripped by pickWritable(), which blocks the plan's own Tier 1 "all blog-related tests pass" criterion. | bug | blog-hero-image-to-thumbnail.md | 2026-07-25 | resolved |
| 116 | setup/PLANS/New/breadcrumbs-blog-essay-response-challenge.md, frontend/assets/css/components/breadcrumbs.css, frontend/assets/js/evidence-detail.js | Plan-review pass on breadcrumbs-blog-essay-response-challenge.md found its canonical reference implementation (evidence-detail.js `renderBreadcrumbs()`, lines ~89-99) emits `.breadcrumbs__sep` / `.breadcrumbs__current` classes that don't exist in breadcrumbs.css — the stylesheet only defines `.breadcrumb-item`, `.breadcrumb-separator`, and `.breadcrumb-item.current` — so following the plan as written produces unstyled breadcrumbs on all five newly-wired page types. | bug | breadcrumbs-blog-essay-response-challenge.md | 2026-07-25 | resolved |
| 117 | setup/PLANS/New/css-breakpoint-tokens.md, frontend/assets/css/layout/grid.css, frontend/assets/css/pages/journal-two-column.css, setup/STYLE_GUIDE/foundations.md | Plan-review pass on css-breakpoint-tokens.md found it defines only 4 breakpoint tokens (480/768/1024/1536px) but `grid.css` and `journal-two-column.css` also hardcode an active 1280px breakpoint that STYLE_GUIDE foundations.md §4 documents as canonical alongside the other four — implementing the plan as written would mark Issue #109 resolved while leaving 1280px hardcoded. Declined as non-trivial and left for the plan author: vague placement instructions for the new style-guide note (task 3), and a naming mismatch between the plan's `--bp-mobile`/`--bp-tablet` token names and the guide's "sm"/"md" labels. | CSS-3 | css-breakpoint-tokens.md | 2026-07-25 | resolved |
| 118 | setup/PLANS/New/esv-evidence-verse-enhancement.md | Plan-review pass on esv-evidence-verse-enhancement.md found its Tier 2 playbook assumes an evidence detail page "that has a primary verse" can simply be opened, but the local database is empty per CLAUDE.local.md — the plan needs a step to create a test evidence record first, or an alternative DOM-injection check that doesn't depend on a real record, to actually stay a Tier 2 (not Tier 3) check. | warning | esv-evidence-verse-enhancement.md | 2026-07-25 | resolved |
| 119 | setup/PLANS/New/frontend-util-test-imports.md | Plan-review pass on frontend-util-test-imports.md found its Notes section describes `figure-orientation.test.js` as already using `await import()`, but that file does not exist yet — it is created by the not-yet-implemented evidence-image-standardisation.md plan — so the wording should say "will use" rather than implying the file currently exists. Does not block implementation since the pattern itself is spelled out directly in the task descriptions. | warning | frontend-util-test-imports.md | 2026-07-25 | resolved |
| 120 | setup/PLANS/New/identifiers-p1-db-api.md, database/schema.sql | Plan-review pass on identifiers-p1-db-api.md found it adds `title`/`external_url` columns to the `identifiers` table via a new migration but has no task to fold the same columns into `database/schema.sql` (the authoritative schema per CLAUDE.local.md) — without it, a database created fresh from schema.sql lacks the columns, repeating the same drift already logged as row #100 for `news_article_thumbnail`. Declined as non-trivial: moving the migration-number-determination note from Notes into the task body, adding schema.sql to Files touched, and repositioning an inline HTML comment about Tier 3 support. | SQL-4 | identifiers-p1-db-api.md | 2026-07-25 | resolved |
| 121 | setup/PLANS/New/identifiers-p2-admin-page.md, admin/assets/js/admin-layout.js | Plan-review pass on identifiers-p2-admin-page.md found its sidebar-integration task instructs adding links by editing `edit-[id].html` files directly, but those editor pages build their sidebars dynamically via `AdminLayout.injectSidebar()` reading the NAV_ITEMS array in `admin/assets/js/admin-layout.js` — the plan never mentions updating NAV_ITEMS and omits admin-layout.js from Files touched, so following it as written would not add the link to any editor page. Four stale file paths in the plan pointing at nonexistent editor files (`admin/contextual-essays/`, `admin/debate/responses/`, `admin/debate/historiography/`, `admin/news-and-blog/blog/` edit-[id].html) were corrected in-place during this review to the real paths (`admin/essays/`, `admin/debate/`, `admin/historiography/`, `admin/blog/`); `admin/resources/sources/edit-[id].html` was left as-is since no such edit page exists at all and removing it is a scoping decision, not a path typo. | bug | identifiers-p2-admin-page.md | 2026-07-25 | resolved |
| 122 | setup/PLANS/New/identifiers-p3-frontend.md, frontend/assets/js/evidence-detail.js | Plan-review pass on identifiers-p3-frontend.md found Task 2 ("Update evidence-detail.js identifier panel rendering") assumes an existing info-row rendering of identifiers that does not exist — `getIdentifierLabel` is imported but never called anywhere in evidence-detail.js — so the task needs reframing as building new info-row rendering (with DOM structure/selectors specified), and Task 3's call-site audit needs to be sequenced after it rather than run in parallel. | bug | identifiers-p3-frontend.md | 2026-07-25 | resolved |
| 123 | setup/PLANS/New/maps-pin-clustering.md, setup/Issues.md | Plan-review pass on maps-pin-clustering.md found it fixes the map-pin-overlap defect described in Issues.md row #72 but has no explicit task to update that row's Status from open to resolved once verified — the Completion Protocol assumes it happens but the task checklist never states it. | warning | maps-pin-clustering.md | 2026-07-25 | resolved |
| 124 | setup/PLANS/New/print-copy-sidebar-footer-fix.md, setup/Issues.md | Plan-review pass on print-copy-sidebar-footer-fix.md found its close-out task instructs changing Issue #98's Status "from resolved to open," but row #98 is already open (with the reopened root-cause note already appended) — the task is stale and should instead verify #98 is open, then set it to resolved only after this plan's own Tier 2 verification passes. Declined as non-trivial: clarifying that the print.css/footer.css line numbers cited in the root-cause section are specific to the detail-page template rather than universal across all HTML files, and tightening the Files-touched wording to remove ambiguity about which page types (zoom/timeline/maps/challenge) are covered. | warning | print-copy-sidebar-footer-fix.md | 2026-07-25 | resolved |
| 125 | setup/PLANS/New/rich-formatting-essays-quotes.md, frontend/assets/js/debate.js | Plan-review pass on rich-formatting-essays-quotes.md found the "Render challenge summaries" task says to run summaries through `renderMarkdown()` but doesn't address that debate.js's `html` tagged-template literal auto-escapes interpolated strings — without SafeString wrapping (or an innerHTML assignment after card creation) the rendered markdown HTML will be double-escaped and shown as literal tags instead of formatted text. | JS-6 | rich-formatting-essays-quotes.md | 2026-07-25 | resolved |
| 126 | setup/PLANS/New/spellcheck-fixes-and-fuzzy-suggestions.md | Plan-review pass on spellcheck-fixes-and-fuzzy-suggestions.md found its Soundex/keyboard-proximity/suggestion-diversity tasks (Issue 3) name the features without algorithmic detail an implementing agent needs — "implement the Soundex algorithm" doesn't specify the variant or whether codes are precomputed, the QWERTY adjacency map and its substitution penalty aren't given, and "top Soundex matches" doesn't say how many to include or how to rank them. | warning | spellcheck-fixes-and-fuzzy-suggestions.md | 2026-07-25 | resolved |
| 127 | frontend/assets/js/utils/markdown.js, frontend/assets/js/utils/content-markers.js | Discovered while implementing rich-formatting-essays-quotes.md: `renderMarkdown()` joined its output blocks with a single `\n`, but `parseContentBody()` only splits blocks on `\n\n+` — so any markdown input producing more than one block (e.g. a heading followed by a paragraph, or two paragraphs) collapsed into one `.split()` entry. Non-`<p>` leading blocks skipped inline-marker resolution entirely, silently dropping `[mla:N]`/`[id:N]` citations in every paragraph after the first heading/list/table; a leading `<p>` block would instead be corrupted by `trimmed.slice(3, -4)` stripping only the outer tags of the whole blob. This affected `blog-detail.js`'s existing `renderMarkdown()` → `parseContentBody()` pipeline in production before this plan touched anything. Fixed as part of this plan (list/blockquote blocks are now built as a single string per block and the final join uses `\n\n`) since it directly blocked verifying the new `useMarkdown` option end-to-end. | bug | rich-formatting-essays-quotes.md | 2026-07-26 | resolved |
| 128 | api/tests/mla.test.js, frontend/assets/js/utils/mla.js | Discovered while implementing mla-citation-formatting-fix.md: `api/tests/mla.test.js` reproduces `formatMlaCitation()` inline (a "synced copy", per its own header comment) rather than importing the real ES module — the same defect class already logged as row #110 for `excerpt.test.js` and `markdown.test.js`, but for a third file that row's audit missed. Its assertions only check for non-empty output and specific stale-field exclusions, never the exact citation punctuation, so this plan's fix (single quotes around article/page titles, italics around journal/website names) landed with all 13 existing tests still green — the suite cannot catch a future formatting regression or confirm this one was applied. Not fixed here: converting the copy to a dynamic `import()` of the real module is the same fix scoped for row #110 and belongs in that follow-up pass, not folded into an unrelated formatting plan. | JS-2 | mla-citation-formatting-fix.md | 2026-07-26 | resolved |
| 129 | frontend/assets/js/blog-detail.js | Discovered while implementing mla-citation-formatting-fix.md: `renderBibliography()` interpolated `formatMlaCitation()`'s HTML output (`<em>Title</em>`, etc.) through the `html` tagged template literal, which escapes every non-`SafeString` value — so blog post bibliography entries rendered literal `&lt;em&gt;` text instead of italics for any citation containing a book title (this predates this plan; book titles were already wrapped in `<em>` before the formatting fix, so the bug was live in production). Every other content type's bibliography renderer (`essay-detail.js`, `response-detail.js`, `historiography-detail.js`, `challenge-detail.js`) builds the `<li>` with a plain JS template literal instead of `html`, so they were unaffected. Fixed by importing `raw` from `./utils/templates.js` and wrapping the citation as `${raw(citation)}`. | JS-6 | mla-citation-formatting-fix.md | 2026-07-26 | resolved |
| 130 | frontend/assets/js/response-detail.js, frontend/debate/responses/ | Discovered while implementing breadcrumbs-blog-essay-response-challenge.md: the plan specifies the response breadcrumb's "Responses" segment link as `/debate/responses/`, but that directory has no `index.html` (only `[slug].html`) — unlike academic/popular challenges, which have `academic-challenges.html`/`popular-challenges.html` list pages. The breadcrumb link as implemented (per plan) will 404 until a responses listing page is built or the plan's target href is revised. | warning | breadcrumbs-blog-essay-response-challenge.md | 2026-07-26 | resolved |
| 131 | frontend/debate/historiography/[slug].html, frontend/debate/academic-challenges/[slug].html | Discovered while generating breadcrumbs-historiography.md: `news-and-blog/blog/[slug].html`, `contextual-essays/[slug].html`, and `debate/responses/[slug].html` all use `<footer class="site-footer">`, but `debate/historiography/[slug].html` and `debate/academic-challenges/[slug].html` use a plain `<footer>` with no class — an inconsistency unrelated to breadcrumbs, found while comparing detail-page templates for the reference `<main>` structure. Not fixed here: out of scope for a breadcrumbs-only plan; needs its own pass to confirm whether `.site-footer` carries functional/visual meaning (e.g. print.css's `body > footer.site-footer` selector at print.css:50) that these two page types are silently missing. | warning | breadcrumbs-historiography.md | 2026-07-26 | resolved |
| 132 | frontend/assets/js/esv_verse.js, frontend/assets/js/evidence-detail.js | Discovered while implementing esv-evidence-verse-enhancement.md: `esv_verse.js`'s `init()` only ran once, on `DOMContentLoaded`, and queried `document.querySelectorAll('[data-esv-ref]')` at that instant. On the evidence detail page the verse `<p>` elements are inserted later, after `getEvidenceBySlug()`'s async fetch resolves and `renderHero()` runs — which happens after `DOMContentLoaded` has already fired — so the module's own auto-init would find zero elements and the feature would silently never enhance anything on this page, despite the plan's audit concluding no change to `esv_verse.js` was needed. Fixed as part of this plan: exported a new `enhanceEsvVerses(root)` from `esv_verse.js` (used by both its own auto-init and by callers targeting a dynamically-rendered subtree) and had `evidence-detail.js`'s `renderHero()` call it directly on the `$verse` container right after setting `innerHTML`. | bug | esv-evidence-verse-enhancement.md | 2026-07-26 | resolved |
| 133 | frontend/assets/css/components/figures.css | Discovered while implementing evidence-image-standardisation.md: the plan's own "How orientation is resolved" design (`width: min(100%, 720px)` + `height: auto` + `max-height: 480px`) does not preserve aspect ratio for a portrait image, contrary to the plan's explicit claim. Per CSS 2.1 §10.4/§10.6.4, the width-recomputed-from-ratio behaviour only triggers when width is *also* `auto` — with a definite `width` (a `min()` expression is definite, not `auto`), the browser instead clips the used height to `max-height` and leaves width fixed, so the `<img>`'s default `object-fit: fill` stretches the pixel content to fill the mismatched box. Confirmed live during Tier 3 verification on a real 1136×1246 portrait upload (`protoevangelium`): the rendered box measured exactly 720×480 instead of the correct 438×480, visibly distorting the image. Fixed as part of this plan: changed `.figure-standard img` to `width: auto; max-width: min(100%, var(--figure-standard-width))` (both dimensions auto), which correctly derives the non-binding dimension from the intrinsic ratio in both orientations — re-verified live post-fix. | bug | evidence-image-standardisation.md | 2026-07-26 | resolved |
| 134 | frontend/assets/js/evidence-detail.js, frontend/assets/js/esv_verse.js, api/scripts/version-assets.js | Discovered while Tier-3-verifying evidence-image-standardisation.md on the live site: a real browser (not the sandboxed preview) threw `SyntaxError: The requested module './esv_verse.js' does not provide an export named 'enhanceEsvVerses'` on `/evidence/the-logos`, from a stale locally-cached copy of `esv_verse.js` predating esv-evidence-verse-enhancement.md's fix (Issues.md #132). Root cause: `version-assets.js` only stamps `?v=<git-hash>` on `<script src>`/`<link href>` attributes in HTML — it has no way to see or rewrite the *internal* ES module specifier `import { enhanceEsvVerses } from './esv_verse.js'` inside `evidence-detail.js` itself, so that particular request is never cache-busted. Combined with `/assets/js/*.js` being served `Cache-Control: public, max-age=31536000, immutable`, any browser that loaded the page before a JS module's internal dependency changed keeps the stale dependency for up to a year, immune to normal reload/revalidation (`immutable` tells the cache not to even check). A hard reload (bypass-cache) fixed the one affected browser for this check, but any real visitor with an already-warmed cache would hit the same failure silently until their cache naturally expires. Not fixed here: out of scope for an image-sizing plan; needs its own fix — likely rewriting internal relative import specifiers to versioned URLs at build/deploy time (mirroring what `version-assets.js` already does for HTML), or dropping `immutable` from the cache-control header for `assets/js/*.js` so at least a hard reload works for affected users. | bug | evidence-image-standardisation.md | 2026-07-26 | resolved |
| 135 | admin/assets/js/admin-insert-image.js | Discovered while finishing spellcheck-fixes-and-fuzzy-suggestions.md: a prior uncommitted-then-committed edit (admin-test-import-real-modules.md, commit c0b70f8) left a duplicated copy of `insertAtCursor()`'s body dangling after the function's own closing brace, producing a JS syntax error that broke the entire file — since it's a classic script (not a module), the syntax error prevented `AdminInsertImage` from initializing at all, taking down the "Insert Image" button across every admin editor (Evidence, Blog, Essay, Historiography, Response) from the moment that commit deployed. Caught because it also crashed `node --test tests/admin-insert-image.test.js` with a top-level syntax error (not a normal test failure). Fixed by deleting the duplicated lines; verified live post-fix (the Insert Image button renders correctly on the Evidence editor). | bug | spellcheck-fixes-and-fuzzy-suggestions.md | 2026-07-26 | resolved |
| 136 | setup/Wikipedia algorithm v2/Wikipedia_alogrithm_refractor.md | Discovered while generating wikipedia-v2-08-skill-documentation-update.md: the refactor spec states in two places (§3.2 "The embedding model, the vector indexes, and every ML dependency live on developer machines only. They never ship to the VPS and never appear in deploy.sh" and §13 "the VPS never runs scoring, so it has no use for [the threshold config]") that vector stores are dev-machine-only artefacts, but the plan-generation brief for this plan stated as a verified fact that "vector stores are rsync'd to the VPS (see Plan 9, being written in parallel)." These two sources directly conflict on whether vector-store indexes ever leave the developer machine. Not resolved here: this plan documents the rsync step in skill.md per the brief's instruction without adjudicating the conflict, since reconciling it requires either correcting the refactor spec or confirming Plan 9 introduces a new requirement (e.g. serving per-signal explainability data live) that supersedes §3.2/§13's dev-machine-only design constraint. | warning | wikipedia-v2-08-skill-documentation-update.md | 2026-07-27 | resolved |
| 137 | api/routes/wikipedia.js | Discovered while generating wikipedia-v2-09-vps-vector-store-serving.md: every existing route in this file (`GET /wikipedia`, `GET /wikipedia/admin`, `GET /wikipedia/:slug`, `POST /wikipedia`, etc.) uses raw `res.status(500).json({ error: '...' })` instead of the project's canonical `sendError`/`error-codes.js` encoding layer described in `setup/Website_guide.md` § Error Notification. Not fixed here: the new route this plan adds (`GET /api/wikipedia/related`) uses `sendError` correctly, but retrofitting the file's pre-existing routes is out of scope for a serving-infrastructure plan and would violate SR-1/scope discipline bundled into an unrelated change. | warning | wikipedia-v2-09-vps-vector-store-serving.md | 2026-07-27 | resolved |
| 138 | setup/SKILLS/!TheJesusWebsite-Wikipedia/scripts/extract.js | Discovered while implementing wikipedia-v2-02-harvest-and-extraction.md: the plan's task list (tasks "Remove section bucketing logic" and "Update extract.js return object") both instructs deleting heading-based section bucketing (classifyHeading/TreeWalker/buckets) AND instructs keeping jesusSeminarInData/InInterp/InOther, mythicistInData/InInterp/InOther, criticalScholarInData/InInterp/InOther, and evangelicalInInterp in the dormant-fallback output — but those placement fields are only computable from the now-deleted heading buckets. The plan's own Notes section explicitly resolves this in the other direction: "the dormant fallback detectors in extract.js extract raw counts and names only, without computing placement... extract.js does not revive heading-based bucketing under any path." Implemented per the Notes resolution: dormant balanced-debate/confessional-balance/Jesus-Seminar/mythicist detectors now scan full article text and return raw counts only (no InData/InInterp/InOther fields); `evangelicalInInterp` was renamed to `evangelicalHit` since it no longer measures placement. Per-hit placement for these dormant families, when activated, must be resolved in rank_engine.py against Plan 4's bucket-labels.json, not recomputed here. Also, the dormant miracle-criticism detector's original heading-based section exclusion (criticism/historical/scholarly/skeptical headings) could not be preserved for the same reason and now scans the full article text, which will reduce its precision if ever activated. | warning | wikipedia-v2-02-harvest-and-extraction.md | 2026-07-27 | resolved |
| 139 | database/scoring-export.json | Discovered while implementing wikipedia-v2-03-gold-set.md: the plan (and rank_engine.py) treat `Wikipedia Articles.csv` as the authoritative ranked-255 source, but that file does not exist on this machine (only present via a Dropbox path referenced in rank_engine.py that is also absent here — see setup/Issues.md #138 context from the prior plan). Used `database/scoring-export.json` (the exported ranked-255 scoring data, tracked in the main git repo) as a substitute for gold-set article selection instead: verified its 255 titles/rankings/category distribution exactly matches the figures this plan's own Notes section cites (is_bible_book: 11, is_passion: 27, is_miracle: 29, is_parable: 41, is_location: 47, is_teaching: 41, none: 69). Not resolved here: `Wikipedia Articles.csv` itself remains missing from this checkout; if a future session runs on a machine with Dropbox access it should reconcile the two files rather than assuming scoring-export.json alone is sufficient going forward. | warning | wikipedia-v2-03-gold-set.md | 2026-07-28 | resolved |
| 140 | Wikipedia: Historical reliability of the Gospels / Historicity of the Gospels | Discovered while hand-labelling the section-classifier gold set (wikipedia-v2-03-gold-set.md): these two titles are both present as distinct entries (rankings 117 and 118) in the ranked-255 list (database/scoring-export.json), but WebFetch of both URLs returns byte-identical article content — same paragraphs, same section headings, same 55-paragraph structure. Appears to be a soft-redirect or content fork on Wikipedia's side rather than a bug in this project's data. Not resolved here: both were labelled identically in gold-set-section-classifier.csv (with a note flagging the duplication) since the ranked-255 source list treats them as separate titles and this plan's scope is labelling, not curating the candidate pool. A future plan touching pool curation (Stage 1/2, per rank_engine.py's docstring) should decide whether one of the two should be excluded to avoid double weight in the ranked list. | warning | wikipedia-v2-03-gold-set.md | 2026-07-28 | resolved |
| 141 | setup/Wikipedia algorithm v2/classifier/ | Section classifier tier accuracy is 0.303 (10/33) against the Plan 3 gold set, well below the §11.2 acceptance bar of ≥0.85. Root cause: MiniLM embedding model's discriminative power is insufficient — cosine similarities between Wikipedia paragraphs and seed exemplars cluster at 0.45–0.65, too tight for clean thresholding at this exemplar set size. Three failure modes: data paragraphs undetected (~30%), separation ratio too low (~30%), too few class-bearing paragraphs (~20%). Full analysis in `setup/Wikipedia algorithm v2/VALIDATION_REPORT.md`. Recommendation there is to proceed to Plans 5/6 anyway (classifier is directionally correct and better than random: 0.303 vs 0.25 baseline), deferring improvement to a later pass (expand exemplars, quantize the model, re-label gold set with matching segmentation). | warning | wikipedia-v2-04-section-classifier.md | 2026-07-29 | resolved |
| 142 | database/scoring-export.json, deploy.sh | Discovered while implementing wikipedia-v2-06-scoring-ranking-export.md: the plan requires migrating database/scoring-export.json (254 real articles) to the new 25-signal contribution shape, but that requires Plan 4's bucket-labels.json and Plan 5's vector-family-scores.json, neither of which exists yet (Plan 4's classifier already failed its §11.2 acceptance bar per row #141; Plan 5's family export.py has never been run against the full corpus). The authoritative harvested detail data (Wikipedia Articles - Scoring Detail.csv) also lives only in a Dropbox path not present on this machine, so there is no way to recompute real 25-signal contributions for the 254 live articles in this session. Not resolved here: api/scripts/import-wikipedia-scoring.js, its 90 tests, and setup/SKILLS/!TheJesusWebsite-Wikipedia/scripts/rank_engine.py were fully migrated to the 25-key rubric and verified via a synthetic Tier-1 smoke test, but database/scoring-export.json was deliberately left in the old 28-key shape rather than fabricated, since deploy.sh runs `set -euo pipefail` and the import script now rejects 28-key exports outright — pushing the new import script without a matching 25-key export would abort the entire deploy (sitemap, pages, pm2 restart included), not just the Wikipedia import step. This plan's code changes are held back from `main` until database/scoring-export.json can be regenerated for real (Plans 4/5 must actually run against the full corpus first). | JS-2 | wikipedia-v2-06-scoring-ranking-export.md | 2026-07-29 | resolved |
| 143 | setup/Wikipedia algorithm v2/Wikipedia Articles.csv | Discovered while implementing wikipedia-v2-06-scoring-ranking-export.md's "Write Wikipedia Articles.csv" task: this deliverable (title/url/ranking, comma→hyphen substituted, %2C-encoded URLs) is written by rank_engine.py's write_files() to MAIN_CSV, a path under a Dropbox folder (/Users/lukeishammacbookair/Library/CloudStorage/Dropbox/_Lukeatron/...) that does not exist on this machine. Not resolved here: the write logic itself is implemented and unchanged by this plan's 25-signal migration; the file simply cannot be generated in this environment. Deferred — gitignored, dev-machine-only convenience artifact, no production impact (database/scoring-export.json is the git-tracked deliverable that drives the live site and was migrated and pushed). Requires running rank_engine.py's CLI on a machine with the Dropbox path mounted. | JS-2 | wikipedia-v2-06-scoring-ranking-export.md | 2026-07-29 | resolved |
| 144 | setup/Wikipedia algorithm v2/wiki-bulk-paste.txt | Discovered while implementing wikipedia-v2-06-scoring-ranking-export.md's "Write wiki-bulk-paste.txt" task: same blocker as issue for Wikipedia Articles.csv — write_bulk_paste_file() in rank_engine.py writes to BULK_PASTE_TXT, also a path under the same missing Dropbox folder. Not resolved here: write logic implemented and unchanged; file cannot be generated in this environment. Deferred — gitignored, dev-machine-only, no production impact. Requires running rank_engine.py's CLI on a machine with the Dropbox path mounted. | JS-2 | wikipedia-v2-06-scoring-ranking-export.md | 2026-07-29 | resolved |
| 145 | setup/Wikipedia algorithm v2/Wikipedia Articles - Scoring Detail.csv | Discovered while implementing wikipedia-v2-06-scoring-ranking-export.md's "Write Wikipedia Articles - Scoring Detail.csv" task: same blocker — the 41-column detail CSV (raw signals, contributions, caps per article) is written to DETAIL_CSV, also a path under the same missing Dropbox folder. Not resolved here: write logic implemented and unchanged; file cannot be generated in this environment. Deferred — gitignored, dev-machine-only, no production impact. Requires running rank_engine.py's CLI on a machine with the Dropbox path mounted. | JS-2 | wikipedia-v2-06-scoring-ranking-export.md | 2026-07-29 | resolved |
| 146 | setup/PLANS/New/wikipedia-v2-09-vps-vector-store-serving.md | Discovered while implementing wikipedia-v2-09-vps-vector-store-serving.md: the plan's draft assumed the FAISS stores under `setup/Wikipedia algorithm v2/vector-stores/` hold per-article embeddings (mapping a sidecar-returned id back to a Wikipedia article via `wikipediaModel.getBySlug`), but they actually hold hand-authored calibration exemplars per family (a few dozen reference passages each, per Wikipedia_alogrithm_refractor.md §3.4.1's nearest-neighbour-label rule) — there is no per-article embedding store anywhere in Plans 1-8. "Find Wikipedia articles related to this one" is not buildable from existing data. Resolved by asking the user: repurposed the endpoint as a live signal-classify tool (`POST /wikipedia/signal-check`, free text + family in, nearest exemplar + fire/no-fire verdict out) instead of a related-articles lookup. A genuine related-articles feature would need a new per-article embedding pipeline — not attempted here, would need its own plan. | warning | wikipedia-v2-09-vps-vector-store-serving.md | 2026-07-29 | resolved |
| 147 | .gitignore, vector-sidecar/ | Discovered while implementing wikipedia-v2-09-vps-vector-store-serving.md: the plan's draft placed the sidecar's FastAPI source at `setup/Wikipedia algorithm v2/vector-sidecar/` and its own "Push to GitHub" task assumed that path was an ordinary tracked repo file. It is not — `.gitignore`'s `/setup/*` blanket rule only re-includes `!/setup/Wikipedia algorithm` (the v1 directory, singular, no "v2"), the same gitignore gap Plan 4 already found for `requirements.txt`/`.python-version`. Anything under `setup/Wikipedia algorithm v2/vector-sidecar/` would have been silently gitignored, so the sidecar code would never have reached GitHub or the VPS via the plan's own deploy path. Resolved by moving the sidecar to a top-level `vector-sidecar/` directory (sibling to `api/`, `mcp-server/`, `scripts/`), which is tracked normally. | bug | wikipedia-v2-09-vps-vector-store-serving.md | 2026-07-29 | resolved |
| 148 | api/routes/wikipedia.js | Per wikipedia-v2-09-vps-vector-store-serving.md's own Error notification section: the new `POST /wikipedia/signal-check` route uses the current canonical `sendError`/`error-codes.js` pattern, but every other route already in `api/routes/wikipedia.js` (GET /, GET /admin, GET /:slug, POST /, PUT /:id, DELETE /, DELETE /:id) still uses the older raw `res.status(500).json({ error: '...' })` pattern. Retrofitting those routes to the encoding layer is out of scope for this plan (unbundled scope creep per SR-1) — logged here per the plan's own instruction rather than silently left unmentioned. | JS-2 | wikipedia-v2-09-vps-vector-store-serving.md | 2026-07-29 | resolved |
| 149 | scripts/sync-vector-sidecar.sh | Discovered while running the live VPS deploy for wikipedia-v2-09-vps-vector-store-serving.md: `pm2 start venv/bin/uvicorn ...` crash-looped in production (`SyntaxError: Invalid or unexpected token` on the file's shebang line) because pm2 defaults to the `node` interpreter for scripts with no recognized file extension, so it tried to execute the Python `uvicorn` binary as JavaScript. Resolved (commit 6d881a7): added `--interpreter none` to the `pm2 start` invocation in `scripts/sync-vector-sidecar.sh`, so pm2 execs the file directly via its own shebang instead of guessing an interpreter. Verified live: `pm2 describe thejesuswebsite-vector-sidecar` now shows `status: online`, `restarts: 0`, and `POST https://thejesuswebsite.org/api/wikipedia/signal-check` returns real results. | bug | wikipedia-v2-09-vps-vector-store-serving.md | 2026-07-29 | resolved |
| 150 | setup/Website_guide.md | Investigated at the user's request re: why wikipedia-v2-08-skill-documentation-update.md's blocker on wikipedia-v2-09-vps-vector-store-serving.md resolved. Found Plan 8's "VPS vector store serving" subsection of the Wikipedia Ranking Pipeline documented Plan 9's ORIGINAL DRAFT design (`GET /api/wikipedia/related?slug=&family=`, mapping sidecar result IDs back to article fields, cache keyed on slug+family, sync-vector-sidecar.sh described as pushing Python source) rather than the as-shipped implementation (`POST /wikipedia/signal-check`, body `{family, text}`, no article mapping — repurposed after discovering the FAISS stores hold calibration exemplars, not per-article embeddings; cache keyed on family+text+k; sync-vector-sidecar.sh only manages venv+pm2 since the code now deploys via git). Plan 8 was apparently completed using Plan 9's draft text before Plan 9's implementation corrected course. Resolved: rewrote the affected paragraph in Website_guide.md to match the real, live-verified endpoint and deploy mechanics. Also noted in passing (not fixed, cosmetic only): Plan 8's own Tier 1 smoke test specified case-sensitive greps for "algorithm lifecycle"/"skill interaction"/"vector embedding database" that return 0 matches against the actual sentence-case headings ("Algorithm lifecycle", etc.) — content is correct and complete, just not literally matching the smoke test's exact-case grep pattern; the checkbox was marked complete without the literal command actually passing. | bug | wikipedia-v2-08-skill-documentation-update.md | 2026-07-29 | resolved |
| 151 | api/scripts/import-wikipedia-scoring.js | Discovered while generating wikipedia-rubric-honesty-and-reproducibility.md: the import script validates signal-key arity, per-signal cap bounds, and the Sum(contributions) == net_score invariant, but has no check that a given signal carries a non-zero value on *any* article. That gap let an export in which four of 25 signals (narrative_interp_split, literary_analysis, maps_diagrams, religious_art) scored 0 for all 254 articles import cleanly and ship to production, where they rendered as permanently empty grid cells indistinguishable from a signal that merely did not fire. The honesty plan fixes the instance (dropping two signals, re-harvesting two) and adds an all-zero assertion to its Tier 1 smoke test, but does not add an automated guard to the import script itself, so a future re-score can silently reintroduce the same class of defect. Suggested fix: have the import script warn (or fail under a strict flag) when any signal_key in the export is zero across every article. | JS-2 | wikipedia-rubric-honesty-and-reproducibility.md | 2026-07-29 | resolved |
| 152 | setup/SKILLS/!TheJesusWebsite-Wikipedia/scripts/rank_engine.py | Discovered while implementing wikipedia-rubric-honesty-and-reproducibility.md's export-regeneration task: `ALGORITHM_DIR` was computed with one dirname() call too many (`dirname(dirname(dirname(dirname(_SCRIPT_DIR))))`), resolving to `<repo>/Wikipedia algorithm` instead of `<repo>/setup/Wikipedia algorithm` — every rank_engine.py command (check/add/remove/exclude/rescore/export) failed immediately with FileNotFoundError on this machine, so the deliverable-relocation task from an earlier session (moving MAIN_CSV/DETAIL_CSV/BULK_PASTE_TXT off the Dropbox path) had never actually been verified working, only written. Separately, `load_main()` never reversed `to_output_title()`'s comma -> " -" CSV encoding when reading titles back in, so any article title containing a comma (e.g. "Mary, mother of Jesus") silently forked into two spellings across main_rows vs. the detail CSV/export/harvest data — this went undetected because no comma-titled article had round-tripped through a rank_engine.py command since the encoding was introduced, and nearly caused a duplicate-article addition during this plan's replacement-candidate selection. Also, `Wikipedia Articles - Scoring Detail.csv` (DETAIL_CSV) did not exist anywhere in this checkout despite `load_detail()`/`cmd_check()`/`cmd_rescore()` all depending on it unconditionally — reconstructed from database/scoring-export.json's raw_signals/contributions/categories fields (net_score/contribution sums verified to match) since no other source of the harvested detail data exists on this machine. Fixed: ALGORITHM_DIR dirname count corrected, load_main() reverses both the title and %2C URL encoding, detail CSV reconstructed and is now tracked under setup/Wikipedia algorithm/. | bug | wikipedia-rubric-honesty-and-reproducibility.md | 2026-07-30 | resolved |
| 153 | frontend/assets/js/utils/wikipedia-signals.js, frontend/assets/js/wikipedia.test.js | Discovered while running this plan's Tier 2 browser check: the row-3 signal key rename (`narrative_interp_split` -> `data_interp_split`) had been applied everywhere, but the *display label* update ("Narrative/interpretation section split" -> "Data/interpretation section split") specified by the same task was only ever applied to `rank_engine.py`'s prose and the spec docs — `SIGNAL_DICTIONARY` in `wikipedia-signals.js` and the mirrored constant in `wikipedia.test.js` still carried the old label, so the live grid tooltip read "Narrative/interpretation section split: not scored" despite the task being checked off complete. Caught by the Tier 2 playbook's DOM-query verification step (`data-tooltip` on a `data_interp_split` cell), not by the JS test suite, since neither test file asserted the label string itself. Fixed: both occurrences corrected to "Data/interpretation section split"; full JS suite re-run clean. | bug | wikipedia-rubric-honesty-and-reproducibility.md | 2026-07-30 | resolved |
| 154 | Wikipedia algorithm/Wikipedia Articles - Reference_the_what.md | Discovered while generating wikipedia-classifier-architecture-diagnosis.md: the user-facing Reference spec diverges from ALGORITHM_GUIDE_the_how.md on two load-bearing scoring details. (1) It documents the placement multipliers (x2 data section, x0.5 interp-only) for rows 19/21 but omits the truncate-toward-zero rounding rule stated in ALGORITHM_GUIDE §6.1 — an implementer working from the Reference alone could floor, ceil, or bankers-round a halved penalty and silently change scores. (2) ALGORITHM_GUIDE §2.9 lists five signals affected by is_passion raised sensitivity (rows 15, 16, 21, 22, 23) but the Reference documents only four, omitting row 15 (Religious art) and its wide-picture test. Not fixed here — this plan's scope is the row-3 classifier architecture, not rubric documentation reconciliation. Both documents also defer to Wikipedia_alogrithm_refractor.md §9 as authoritative without either including it, so divergences cannot currently be adjudicated from the two files alone. | warning | wikipedia-classifier-architecture-diagnosis.md | 2026-07-30 | resolved |
| 155 | Wikipedia algorithm/gold-set-section-classifier.csv | Discovered while generating wikipedia-classifier-architecture-diagnosis.md: the section-classifier gold set yields only 33 scorable articles, so the >=0.85 tier-accuracy acceptance gate is measured with roughly +/-0.12 of sampling noise at 95% confidence — a future run reporting 0.85 could have a true accuracy near 0.73. Compounding this, calibrate.py's choose_best() maximises accuracy across all 33 articles with no held-out split, so any reported figure is an in-sample optimum. This plan adds bootstrap CI reporting so the number is at least read honestly, but deliberately does not expand the gold set (the follow-on Signal 3 plan owns that, and its current draft also only relabels the same ~40 articles without adding any). Not resolved: the gate remains statistically underpowered until the gold set grows. | warning | wikipedia-classifier-architecture-diagnosis.md | 2026-07-30 | open |
| 156 | Wikipedia algorithm/vector-family-thresholds.yaml | Discovered while generating wikipedia-classifier-architecture-diagnosis.md: all nine vector signal families carry precision: 0.0 and f1: 0.0 in the calibrated thresholds file, which is below the 0.80 precision floor, so every family silently falls back to its dormant keyword detector at scoring time. The file's own header comment documents the fallback rule but nothing in the pipeline surfaces that all nine are currently underwater — the condition is indistinguishable, from the shipped export alone, from families that are calibrated and simply not firing. This is the same class of silent-degradation defect as Issues.md #151 (all-zero signals importing cleanly). Not fixed here: this plan's scope is row 3's classifier, not family calibration. Suggested fix: have rank_engine.py warn loudly when it resolves a vec() call to fallback because of a precision-floor violation, and surface the count of underwater families in the export. | warning | wikipedia-classifier-architecture-diagnosis.md | 2026-07-30 | resolved |
| 157 | Wikipedia algorithm/calibrate.py:687-817 (`write_docs()`) | `write_docs()` always appends a new `## Calibration Results` appendix to `CLASSIFIER_CALIBRATION.md` via `cal += appendix` (line 816) instead of replacing the previous run's section — each calibration run adds a duplicate appendix; the file currently has 6 identical copies stacked. The `---` separator before each appendix is also part of the appended string, so stripping from the first occurrence of `\n---\n## Calibration Results` and re-appending the new one would fix it. Also, the function does a `cal.replace()` on a placeholder table that only exists once at the top of the file, so the replace-then-append pattern is a two-phase mismatched strategy — the first run's appendix was fine, but every subsequent run stacks a new copy on top. | bug | wikipedia-signal-3-diagnostic-report.md | 2026-07-30 | resolved |
| 158 | Wikipedia algorithm/README.md | README.md states a third, different dev-machine footprint figure (~201 MB, quantized-model assumption) — inconsistent with both numbers in `CLASSIFIER_CALIBRATION.md` (the top-section hand-measured ~265 MB and the auto-generated appendix ~239 MB). Three different footprint figures now exist across two files with no reconciliation note or measurement-date attribution, so a reader cannot determine which (if any) is current. Not fixed here (out of scope for the signal-3 diagnostic plan), noted for awareness only. | warning | wikipedia-signal-3-diagnostic-report.md | 2026-07-30 | resolved |
| 159 | setup/Issues.md | Row #141's stated root cause is superseded by measurement but its text cannot be edited (rows are append-only except Status). #141 says the section classifier's accuracy problem is caused by "MiniLM embedding model's discriminative power is insufficient"; the 2026-07-30 paragraph-level confusion matrix measured data-vs-interpretation accuracy at 0.662-0.706 (well above the 0.500 chance rate), and identified an un-calibrated register gate rejecting ~50% of the corpus as the dominant error source instead. #141 also references paths under `setup/Wikipedia algorithm v2/` which no longer exist (the directory is `Wikipedia algorithm/` at the repo root). #141 stays open (the >=0.85 gate is still unmet at 0.641) but its diagnosis should not be relied on. | warning | wikipedia-signal-3-live-weight-and-llm-labelling.md | 2026-07-30 | resolved |
| 160 | Wikipedia algorithm/gold-set-section-classifier.csv | The `one_sided` and `unclassifiable` tier states are unmeasurable at the current gold-set size: the 39-article gold set contains exactly 1 `one_side_only` article and 0 `unclassifiable` ones, so both score 0.000 precision and 0.000 recall in every calibration run. This is a degenerate-class artifact, not a code bug (`calibrate.py` correctly maps the gold string `one_side_only` to the internal state `one_sided`). Both weights are set to 0 in the signal-3 live-weight plan rather than guessed. Expanding the labelled corpus may make them measurable; until then they cannot be validated. | warning | wikipedia-signal-3-live-weight-and-llm-labelling.md | 2026-07-30 | resolved |
| 161 | setup/SKILLS/!TheJesusWebsite-Wikipedia/scripts/rank_engine.py | Discovered while implementing wikipedia-signal-3-live-weight-and-llm-labelling.md: running the first full `rescore` in some time (needed to activate Signal 3 for real) revealed `harvest_one()` no longer computes several keyword-detector fields at all (`balancedDebateHits`, `balancedDebateNamedAuthors`, `criticalScholarHits`, `criticalOutsideInterp`, `evangelicalContrast`, and likely others feeding `ot_nt_criticism`/`supernatural_criticism`/`secular_materialist`/`gnostic_over_emphasis`) — the keys are absent from its returned dict, not merely zero. The previously-committed `scoring-export.json` had healthy non-zero values for these five signals (e.g. `confessional_balance`: 52/255 non-zero), but those were stale: nobody had run a full re-harvest since whatever change silently dropped this logic, so `checkNonPendingSignalsNonZero`'s guard never had a chance to catch it until now. Not fixed here — restoring these keyword detectors is a separate, unrelated undertaking to Signal 3/LLM labelling and was not attempted. Blocks a clean `rank_engine.py rescore` + import until either the detectors are restored or these five signals are (temporarily) added to `PENDING_SIGNAL_KEYS`. | bug | wikipedia-paragraph-label-reuse.md | 2026-07-31 | resolved |
| 162 | Wikipedia algorithm/calibrate.py, setup/SKILLS/!TheJesusWebsite-Wikipedia/scripts/rank_engine.py | Two independent hardcoded copies of the tier-integer→state-name mapping (`calibrate.py`'s `_contribution_to_state()` and `rank_engine.py`'s `merge_upstream_signals()`) keyed on the pre-2026-07-30 weight scheme (`{10: "clear_split", -5: "muddled", 0: "one_sided"}`). When `classifier/config.py`'s `TIER_CLEAR`/`TIER_MUDDLED` changed to +3/0 on 2026-07-30, neither copy was updated: every genuine `clear_split` article (tier=3) missed both tables' keys and silently fell back to `unclassifiable` (0 contribution). This collapsed `calibrate.py`'s measured tier accuracy to 0.026 and made every production `data_interp_split_contribution` score 0 regardless of the article's real tier — the root cause of the reverted 2026-07-30 production deploy. Found 2026-07-31 while investigating the 0.026 accuracy regression. Fixed same day: `calibrate.py` now delegates to `classifier.scorer._tier_state_name()` instead of duplicating the table; `rank_engine.py` reads `bucket-labels.json`'s own `tier_state` field directly instead of reconstructing it from the bare integer. Logged here (rather than only in the commit message) so the failure mode — a second hardcoded copy of a scheme that lives in `classifier/config.py` — is visible to whoever next changes `TIER_CLEAR`/`TIER_MUDDLED`. | bug | wikipedia_finalcleanup_plan.md | 2026-07-31 | resolved |
| 163 | Wikipedia algorithm/bucket-labels.json, Wikipedia algorithm/labels-corpus.json | Discovered while auditing the Wikipedia doc set for wikipedia_finalcleanup_plan.md: `CLASSIFIER_ARCHITECTURE_DECISION.md` Decision 5 recommended making LLM labels (`labels-corpus.json`, 0.926 human-agreement, passes the 0.85 gate) the production source for Signal 3, with the embedding classifier (`bucket-labels.json`, 0.641 tier accuracy, fails the same 0.85 gate) demoted to a secondary/cross-check role. `LLM_LABELLING.md` records the decision as accepted and the full corpus as labelled. But no code path converts `labels-corpus.json` into `bucket-labels.json`'s schema — `classifier/export.py`'s `export_article()` (the only writer of `bucket-labels.json`) calls the embedding classifier directly, never reads `labels-corpus.json`. `bucket-labels.json`'s mtime (13:28) is later than `labels-corpus.json`'s (11:17) on 2026-07-31, confirming it was regenerated from the classifier, not derived from the LLM labels. So Signal 3 is currently live in production (per the latest commit) scored by the *lower*-accuracy, gate-failing embedding classifier, not the validated, gate-passing LLM labels the labelling effort was built to supply. Not fixed here — deciding whether to wire `labels-corpus.json` into `bucket-labels.json`'s schema (or replace it outright) is a scope decision for Luke, not a documentation fix. | bug | wikipedia_finalcleanup_plan.md | 2026-07-31 | resolved |
| 164 | Wikipedia algorithm/CLASSIFIER_ARCHITECTURE_DECISION.md (deleted), Wikipedia algorithm/LLM_LABELLING.md (deleted) | Doc-hygiene pass for wikipedia_finalcleanup_plan.md: both files were one-time decision/process records, all of whose decisions are now settled and implemented. Deleted after folding their still-load-bearing content elsewhere: (1) `CLASSIFIER_ARCHITECTURE_DECISION.md`'s Decision 3 (the settled +10/−5/0/0 tier weighting and its 8-location propagation checklist) → `Wikipedia algorithm/OPERATOR_GUIDE.md` and `ALGORITHM_GUIDE_the_what.md` §9; Decision 1 (four-store/three-tier architecture) and Decision 5 (LLM-labels-as-production-source, only partially implemented — see #163) → `ALGORITHM_GUIDE_the_how.md`'s Stage 4 description. Decisions 2 and 4 (block-structure separation functional, scoring-rule-before-model ordering) are process history with no standalone operational fact left to preserve; the *outcomes* (adjacency mode and centroid scoring rule are what's actually configured) are already stated in `CLASSIFIER_CALIBRATION.md`'s auto-generated results, so nothing was lost. (2) `LLM_LABELLING.md`'s operational facts (model selection `deepseek-v4-flash` over `-pro`, the 0.85/0.75 agreement gate, the `--stale` re-labelling command, cost ~$0.18/corpus run) → `ALGORITHM_GUIDE_the_how.md` Stage 4. | warning | wikipedia_finalcleanup_plan.md | 2026-07-31 | resolved |
| 165 | Wikipedia algorithm/Wikipedia Articles.csv, Wikipedia algorithm/scripts/rank_engine.py | Relocated from `ALGORITHM_GUIDE_the_what.md`'s "Data/scope notes" (a reference doc, not a log) during the wikipedia_finalcleanup_plan.md doc-hygiene pass. Two same-day 2026-07-30 exclude+topup runs, both net 255→255 with no shortfall: (1) excluded *Christ myth theory*, *Church of the Holy Sepulchre*, *Textual variants in the Gospel of John*, *List of gospels*, *Gospel of Philip*, *Gospel of James*, and (after being added and reconsidered) *Shroud of Turin*; replaced with *Mount Tabor*, *Praetorium*, *Mara bar Serapion on Jesus*, *Caesarea Maritima*, *Chorazin*, *Magdala*. Also fixed a `rank_engine.py` bug found mid-run: `_REPO_ROOT` had one extra `dirname()` call, resolving to the parent of the repo instead of the repo root, silently breaking the `scoring-export.json` → `database/` copy. (2) Second pass, same day: excluded *Church of the Nativity*, *Life of Jesus*, *Four Evangelists*, *Synoptic Gospels* (general-overview/architecture articles); replaced with *Shechem (Sychar)*, *Al-Maghtas*, *Judas the Zealot*, *Nain, Israel*. | warning | wikipedia-v2-* (unspecified) | 2026-07-30 | resolved |
| 166 | setup/PLANS/New/wikipedia-signal-3-activation.md, wikipedia-signal-3-diagnostic-report.md, wikipedia-signal-3-live-weight-and-llm-labelling.md | Marked complete and moved to `PLANS/Completed/` per wikipedia_finalcleanup_plan.md, despite unchecked task boxes in two of the three (same pattern as issues #39/#46). `wikipedia-signal-3-activation.md` has 28 unchecked tasks — expand the gold set past 40 articles, author Tier-1/2/3 exemplars, build a dedicated close-analysis FAISS store, calibrate against the expanded gold set, clear the ≥0.85 gate via the embedding classifier itself — all superseded, not merely skipped: the project pivoted to LLM labelling (`scripts/llm_label_corpus.py`, DeepSeek) instead, which cleared an equivalent 0.85/0.75 agreement gate at 0.926/0.846 without doing any of these tasks. `wikipedia-signal-3-diagnostic-report.md` has 9 unchecked recommendations (paragraph-segmentation fix, gold-set expansion to ~100–120 articles, explicit register features, model swap considerations) — same status, superseded by the LLM pivot rather than completed. Neither plan's classifier-improvement path was actually finished; Signal 3 shipped via a different mechanism. See setup/issues.md #163: the LLM labels that superseded this work are not actually wired into production scoring yet either, so the classifier path being abandoned does not mean the intended end state (gate-passing labels driving Signal 3) has been reached. `wikipedia-signal-3-live-weight-and-llm-labelling.md` has 1 unchecked item — a live Chrome sign-in verification that the `data_interp_split` grid cell shows a real contribution — not completed here (same WebAuthn-can't-be-automated constraint as issues #33/#76/#91/#92); needs a manual check by the site owner. | warning | wikipedia_finalcleanup_plan.md | 2026-07-31 | resolved |
| 167 | Wikipedia algorithm/ALGORITHM_GUIDE_the_how.md, Wikipedia algorithm/ALGORITHM_GUIDE_the_what.md | Discovered during the second-pass verification sweep for wikipedia_finalcleanup_plan.md's doc-hygiene phase: both guides cite numbered sections (§3.1.1–§3.1.10, §9, §11.2–§11.4, §12.2, and similar) as if they resolve within the current doc set, but no file in the repo actually contains headings at those numbers — they're orphaned references to the old `Wikipedia_alogrithm_refractor.md` ("the refactor spec"), which does not exist anywhere in this checkout (confirmed via repo-wide search). `ALGORITHM_GUIDE_the_how.md`'s own heading structure only goes to two levels (`2.1`–`2.9`, `6.1`–`6.3`, `7.1`–`7.8`) and never reaches `§3.1.x`, `§9`, `§11.x`, or `§12.x`. This pass fixed every broken *filename* reference (`Wikipedia_alogrithm_refractor.md`, `Wikipedia Articles - Reference.md`, "the refactor spec") but deliberately did not attempt to resolve the *orphaned section numbers* themselves — mapping ~20 dead `§3.1.x`/`§11.x` citations to real content (or renumbering the guide to actually contain those sections) is a bigger restructuring job than a doc-hygiene pass, and touches section numbers the weights table's own detection-column prose depends on. Not fixed here. A future pass should either build out the missing `§3.1.x` (per-family vector detection detail) and `§9`/`§11`/`§12` (activation checklist, validation criteria, tie-break rule) sections for real, or strip every dead citation down to a plain prose description. | warning | wikipedia_finalcleanup_plan.md | 2026-07-31 | resolved |
| 168 | Wikipedia algorithm/classifier/config.py, classifier/scorer.py, classifier/tests/test_tiers.py, Wikipedia algorithm/scripts/rank_diff.py, setup/SKILLS/!TheJesusWebsite-Wikipedia/scripts/rank_engine.py, api/scripts/import-wikipedia-scoring.js, api/tests/import-wikipedia-scoring.test.js, frontend/assets/js/utils/wikipedia-signals.js, frontend/assets/js/utils/wikipedia-signals.test.js | `data_interp_split`'s tier weighting propagated from the interim scheme (shipped 2026-07-30: `TIER_CLEAR=+3`, muddled/one-sided/unclassifiable all `0`) to the settled target (+10 clear split / −5 muddled / 0 one-sided / 0 unclassifiable) across all 9 locations per `OPERATOR_GUIDE.md`'s checklist. Also fixed a latent bug found along the way: `frontend/assets/js/wikipedia.test.js` already asserted `capMagnitude: 10` for this signal while the actual `wikipedia-signals.js` source still had `capMagnitude: 3` — a pre-existing test/source mismatch (not caught because that file's test never ran the assertion against the real module) that this propagation incidentally fixed. `data_interp_split` is now genuinely bidirectional (can be +10 or −5), unlike every other signal in `SIGNAL_DICTIONARY`; `capMagnitude`/`polarity` follow the same "largest-magnitude extreme wins" convention already used by `referencing_quality`. Full test suites re-run clean: 81 Python classifier unit tests, 115 `import-wikipedia-scoring.test.js` tests, 36 `wikipedia.test.js` tests, 17 `wikipedia-signals.test.js` tests, and a read-only `rank_engine.py check` (255 articles, all files consistent). **Not done, and deliberately so per the plan:** no `rank_engine.py rescore` was run — `Wikipedia Articles - Scoring Detail.csv` and `scoring-export.json` on disk still reflect the interim +3/0/0/0 scoring for every article until a rescore is explicitly requested. | design | wikipedia_finalcleanup_plan.md | 2026-07-31 | resolved |
| 169 | Wikipedia algorithm/Wikipedia Articles.csv, Wikipedia algorithm/scripts/rank_engine.py | Confirmed for wikipedia_finalcleanup_plan.md: the 255-article list (`Wikipedia Articles.csv`) is a fixed canonical set, not regathered from the candidate pool on each run. Verified by reading `rank_engine.py`'s `check`/`add`/`remove`/`exclude`/`rescore` commands end to end — none gates on or targets a specific row count; 255 is simply the list's current size. `check` (the default action per `skill.md` STEP 1, rewritten in this plan's Phase 1) is read-only and never crawls/adds/removes regardless of count. Additions/removals only happen via an explicit request naming article(s) (STEP 0 exclude/remove, STEP 2-4 add) — confirmed the crawl mechanism (Stage 1 in `ALGORITHM_GUIDE_the_what.md`) still exists and works, it's just no longer invoked automatically. No stale "dynamic re-gathering" language found remaining in any doc after this plan's Phases 1-2 fixes (repo-wide grep for "ceiling"/"top up"/"topped up" across `Wikipedia algorithm/` and the skill folder). Added an explicit "List status" confirmation note to `ALGORITHM_GUIDE_the_what.md`. | design | wikipedia_finalcleanup_plan.md | 2026-07-31 | resolved |
| 170 | setup/STYLE_GUIDE/content-patterns.md, frontend/assets/js/arbor/arbor-interactions.js, admin/assets/js/admin-arbor/arbor-canvas.js | Discovered while generating timeline-arbor-style-zoom.md: the Arbor Diagram's live zoom-bounds code (`ZOOM_MIN`/`MIN_SCALE` = 0.05, `MAX_SCALE` = 1.0, button step ×1.25, admin wheel-zoom step ×1.1/×0.9) does not match `setup/STYLE_GUIDE/content-patterns.md` §9 "Arbor Diagram", which documents min 0.25×, max 3×, step 0.25, and never mentions wheel-zoom at all. The zoom plan deliberately matches the live CODE (the confirmed-working reference) rather than the stale style-guide numbers when porting the same model to the timeline. Not fixed here — reconciling the style guide's Arbor Diagram entry with the actual shipped bounds is a separate documentation task. | warning | timeline-arbor-style-zoom.md | 2026-08-02 | resolved |
| 171 | admin/assets/js/admin-arbor/arbor-canvas.js, admin/assets/js/admin-canvas-zoom.js | timeline-arbor-style-zoom.md creates a new shared admin canvas-zoom module (`admin-canvas-zoom.js`, SR-4) consumed by admin-timeline, but deliberately does not migrate `admin-arbor/arbor-canvas.js` onto it — arbor's zoom is confirmed working and migrating it was judged too risky to bundle with the timeline fix. This leaves admin-arbor and admin-timeline with two independent zoom implementations again once the new module exists, the same class of duplication SR-4 exists to prevent. Not fixed here — a future consolidation pass could migrate arbor onto the shared module once it has separately proven stable. | design | timeline-arbor-style-zoom.md | 2026-08-02 | resolved |
| 172 | frontend/assets/css/pages/arbor.css, frontend/assets/css/pages/timeline.css | Discovered while generating timeline-zoom-controls-overlap-fix.md: both `.arbor-canvas` and (after this plan) `.timeline-container` reserve a fixed `min-height: 600px` box for an absolutely-positioned canvas whose real content height is never measured. An unusually tall cluster stack (many staggered dots/labels at one period/node) could in principle exceed 600px and get clipped by the container's `overflow: hidden`, on either diagram. This is a pre-existing latent limitation on Arbor (not introduced by this plan) that the timeline fix deliberately mirrors rather than solves, since no occurrence has been observed in practice and a dynamic-height measurement would be new complexity for an unconfirmed failure mode. Not fixed here — a future pass could measure actual cluster fan-out at render time and grow the container height accordingly if this is ever observed live. | warning | timeline-zoom-controls-overlap-fix.md | 2026-08-03 | open |
| 173 | admin/assets/css/admin-diagrams/arbor-pen-chips.css | Discovered while generating chip-style-parity.md: this admin stylesheet references frontend design tokens (`--bg-primary`, `--border`, `--text-primary`) instead of admin-scoped equivalents. The Website guide states frontend and admin are deliberately separate design systems that must never cross-import; a token that is only defined in the frontend's variables.css will resolve to nothing (or to an unrelated admin value) in admin context. Scheduled for fix by chip-style-parity.md. | CSS-2 | chip-style-parity.md | 2026-08-03 | open |
| 174 | admin/assets/css/admin-diagrams/maps-holding-pen-base.css | Discovered while generating chip-style-parity.md: same cross-system token leak as the row above — uses frontend tokens (`--bg-surface`, `--border`, `--radius-sm`, and also `--duration-fast`/`--ease-out` in its transition declaration) rather than admin-scoped equivalents. The two animation tokens were missed by the initial survey and only caught during plan review. Note `--overlay-subtle`, used in the same file's box-shadow, was initially also flagged as a leak but is NOT one — it is legitimately defined on admin's own `:root` (`admin/assets/css/admin-base/variables.css:114`) and must be left alone. Scheduled for fix by chip-style-parity.md. | CSS-2 | chip-style-parity.md | 2026-08-03 | open |
| 175 | admin/assets/css/admin-diagrams/arbor-pen-badges.css | Discovered while generating chip-style-parity.md: the arbor status badge uses a brown/grey published-vs-draft colour scheme while the table status badge (`admin/assets/css/admin-components/tables.css`) uses green/yellow for the same two states — the same semantic state reads as two different colours depending on which admin screen you are on. The file additionally hardcodes `font-size: 10px`, `text-transform: uppercase`, and `border-radius: 4px`, and leaks the frontend tokens `--radius-sm`, `--accent`, `--text-muted`. Note `--accent-subtle` and `--gray-subtle`, used in the same file, were initially also flagged as leaks but are NOT — both are legitimately defined on admin's own `:root` (`admin/assets/css/admin-base/variables.css:123-124`) and must be left alone. Scheduled for fix by chip-style-parity.md; note the colour-scheme unification is a deliberate visual change to the arbor editor, not a pure refactor. | CSS-2 | chip-style-parity.md | 2026-08-03 | open |
| 176 | admin/assets/css/admin-components/tables.css | Discovered while generating chip-style-parity.md: table status badge hardcodes `border-radius: 12px` and `font-size: 11px` rather than referencing tokens, and both values diverge from every other badge in the codebase (radius elsewhere is 4px or 6px). Scheduled for fix by chip-style-parity.md. | CSS-2 | chip-style-parity.md | 2026-08-03 | open |
| 177 | admin/assets/css/analytics.css | Discovered while generating chip-style-parity.md: `.analytics-chip` hardcodes `padding: 6px 14px` and `font-size: 13px` instead of referencing admin tokens. Scheduled for fix by chip-style-parity.md. | CSS-2 | chip-style-parity.md | 2026-08-03 | open |
| 178 | admin/assets/css/admin-components/resources-list.css | Discovered while generating chip-style-parity.md: `.admin-resources-pen__chip` hardcodes `font-size: 13px` instead of referencing an admin token. Scheduled for fix by chip-style-parity.md. | CSS-2 | chip-style-parity.md | 2026-08-03 | open |
| 179 | admin/assets/css/admin-diagrams/timeline-era-filter.css | Discovered while generating chip-style-parity.md: the era filter chip hardcodes its padding and uses a fallback-bearing font-size declaration (`var(--text-2xs, 0.625rem)`) rather than a token that is actually defined in the admin variables file — the fallback silently masks a missing token. Scheduled for fix by chip-style-parity.md. | CSS-2 | chip-style-parity.md | 2026-08-03 | open |
| 180 | frontend/assets/css/components/filters/filter-chips.css | Discovered while generating chip-style-parity.md: the frontend filter chip has no era-colour highlight, while the admin timeline era filter chip does (3px left-border tint from `--era-*` tokens, full era-colour background when active). Both design systems define the `--era-*` tokens with identical values, so the highlight is achievable on the frontend without any cross-import — it was simply never implemented there. This is the main frontend/admin visual divergence the owner asked about. Scheduled for fix by chip-style-parity.md. | warning | chip-style-parity.md | 2026-08-03 | open |
| 181 | setup/STYLE_GUIDE/components.md | Discovered while generating chip-style-parity.md: §8 documents content badges and status badges but contains no specification for filter chips at all, despite filter chips being used on the timeline, maps, and evidence-search pages and in three admin surfaces. The documented status-badge font-size and border-radius also do not match any shipped implementation. Separately noted while auditing plan A: §8 documents the timeline spine as `1px solid var(--border)` while the shipped CSS uses `height: 2px; background: var(--border-strong)`. Scheduled for fix by chip-style-parity.md (chips) and timeline-line-stability.md (spine). | warning | chip-style-parity.md | 2026-08-03 | open |
| 182 | frontend/assets/js/timeline/timeline-interactions.js, frontend/evidence/timeline/*.html | **Severe, live-production bug — the public Timeline feature never renders on a natural page load, on every era page tested.** Discovered during timeline-zoom-controls-overlap-fix.md's Tier 3 live-Chrome check (`https://thejesuswebsite.org/evidence/timeline/old-testament.html` and `.../pre-incarnation.html`, both reproduced identically): after a fresh navigation, `#loading-state` stays visible indefinitely, `#timeline-container` stays `hidden` with only its static `.timeline-spine` child (no `.timeline-world` ever appended), and `#empty-state` never shows either — i.e. neither the success path nor the `showEmpty()` failure path in `timeline-interactions.js`'s `init()` ever completes, with zero console errors/warnings logged (silent — a JS-2 violation regardless of root cause). Ruled out: the embedded `<script id="timeline-data">` JSON is present and valid (confirmed via direct `JSON.parse` in-page); manually re-invoking the exact same pipeline from the console (`import` `timeline-render.js`/`timeline-data.js`/`api.js`, call `init()` → `readEmbeddedData()` → `groupEventsByPeriod()` → `renderTimeline()`) **succeeds** and renders dots/labels/controls correctly with zero geometry overlap — so the render logic itself is sound and the defect is specifically in the natural bootstrap never reaching/completing that call. Strong lead, not fully proven: the network waterfall shows every timeline module (`timeline-data.js`, `timeline-render.js`, `timeline-zoom.js`, `timeline-interactions.js`, plus shared utils) fetched **twice** per page load — once at its versioned top-level `<script src="...?v=<commit>">` URL, once more at the bare unversioned URL — because relative `import "./x.js"` specifiers inside each module resolve against the *importing* module's own URL and drop its `?v=` query, while the top-level `<script>` tags keep it. This creates two independent module-registry entries (hence two separate sets of module-scope state) for the same file per page, which is very likely implicated but was not traced all the way to a definitive single line. Not fixed here — out of scope for the CSS-only overlap-fix plan that surfaced it, and this needs focused investigation, not a guess. **Impact: real visitors currently cannot see any timeline content on `/evidence/timeline/*` at all**, independent of and unrelated to the overlap-fix plan's own CSS change (verified: the overlap-fix plan touched only `timeline.css`, no JS). | bug | timeline-zoom-controls-overlap-fix.md | 2026-08-03 | resolved |
| 183 | Wikipedia algorithm/tests/validate-gold-set.test.js, Wikipedia algorithm/gold-set-section-classifier.csv, Wikipedia algorithm/gold-set-vector-families.csv, Wikipedia algorithm/gold-set-negative-controls.csv | Gold-set data drift surfaced after fixing validate-gold-set.test.js's REPO_ROOT path (was one dirname() too deep, un-deading the validator). The validator's test 1a (every gold-set article_title must be in the ranked-255 set) and test 1b (negative controls must NOT be in the ranked set) both fail. 1a — 6 article titles appear in the classifier and/or vector-families CSVs but are not in database/scoring-export.json's ranked-255 set; all 6 were deliberately excluded from the list in two same-day top-up runs on 2026-07-30 (see row #165): Gospel of James, Gospel of Judas, Rylands Library Papyrus P52, Life of Jesus, Textual variants in the Gospel of John, List of gospels. These rows must be removed from the gold-set CSVs. 1b — 3 negative-control rows are in the ranked-255 set when they must be out-of-scope: Al-Eizariya (Bethany), I am (biblical term), Resurrection — these were added to the ranked list after the gold set was labelled. The 3 rows must be moved from the negative-controls CSV to the appropriate gold-set file(s), or the negative-controls CSV rows dropped and replaced with genuine out-of-scope candidates. | data | none | 2026-08-03 | resolved |
| 184 | frontend/assets/js/timeline/timeline-render.js, frontend/assets/js/utils/dom.js | `renderTimeline()` performs all its DOM mutation inside `batchWrite()` (rAF-scheduled), so in a backgrounded or hidden tab the timeline legitimately stays in its pre-render state until the tab is fronted. This is correct browser behaviour, not a bug, but it makes the timeline indistinguishable from a silently-stalled init() during automated or headless verification — it caused Issues.md #182 to be logged as a severe production outage on 2026-08-03 and re-diagnosed as a false alarm on 2026-08-04. Note the concrete implication for future work: any Tier 2/Tier 3 verification of a diagram page must front the tab (or take a screenshot first) before asserting on rendered DOM, otherwise it reads pre-render state. Suggest documenting this in the plan template's verification guidance. | warning | none | 2026-08-04 | open |
| 185 | admin/assets/js/admin-arbor/arbor-canvas.js, admin/assets/js/admin-canvas-zoom.js | Row #171's framing is superseded by an explicit owner decision on 2026-08-04. Arbor's independent zoom/pan implementation is now ACCEPTED duplication, not consolidation-pending work. Rationale: SR-4 exists to prevent bug-fix drift between copies, and that risk scales with edit frequency — arbor's zoom is finished, working, and untouched, so the duplication costs nothing while a migration would risk a working feature for no user-visible gain. Mitigation applied instead of migration: reciprocal signpost comments added to both modules' JSDoc headers, so a zoom bug fixed in one is checked against the other. Trigger condition that would justify migrating: any future change to arbor's zoom behaviour (bounds, step, wheel handling), since the file is open for editing at that point and the marginal risk drops sharply. #171 set to resolved on this basis. | SR-4 | none | 2026-08-04 | resolved |

| 186 | setup/ISSUES/Issues.md (row #111) | Row #111 originally named five functions as dead (addToEvidence, removeFromEvidence, getResourcesForEvidence, reorderEvidenceResources, reorderList), but only the first four operated on evidence_resource_lists. reorderList() operates on resources.sort_order and was mis-grouped. Rather than deleting it, this plan wired it up behind a new POST /resources/reorder route, replacing N sequential per-row PUT calls the admin drag-to-reorder feature previously used to persist order with a single bulk transaction. | warning | remove-dead-evidence-resource-lists.md | 2026-08-04 | resolved |